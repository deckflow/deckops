diff --git a/dist/browser.d.ts b/dist/browser.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6c7c7302ad3cd43e77c0553837c53060089c42da
--- /dev/null
+++ b/dist/browser.d.ts
@@ -0,0 +1,1437 @@
+import { AxiosError } from 'axios';
+
+declare const DEFAULT_ROOT = "https://app.deckflow.com/v1";
+declare const DEFAULT_TIMEOUT = 300;
+declare const DEFAULT_POLL_INTERVAL = 2000;
+declare const DEFAULT_CHUNK_SIZE: number;
+/** When creating a task with files under this total size, attach them inline as `files` instead of async upload + `fileIds`. */
+declare const INLINE_TASK_FILES_MAX_BYTES: number;
+declare const DECK_TASK_TYPES: readonly ["file.compress", "image.ocr", "pptx.split", "pptx.join", "pptx.getFontInfo", "pptx.getTextShapes", "pptx.embedFonts", "convertor.ppt2image", "convertor.ppt2pptx", "convertor.ppt2pdf", "convertor.doc2pdf", "convertor.ppt2video", "convertor.pdf2image", "convertor.keynote2image", "convertor.keynote2html", "convertor.keynote2pdf", "convertor.html2png", "convertor.markdown2png", "convertor.html2pptx", "html.buildPlayer", "video.compress", "image.convertWebp", "image.resize", "pdf.pdfParse", "pptx.parse", "docx.parseTextAndImage", "keynote.parseTextAndImage", "html.getByURL", "parse.convert", "generation", "translation", "revamp"];
+type DeckTaskType = (typeof DECK_TASK_TYPES)[number];
+type DeckTaskStatus = 'pending' | 'running' | 'completed' | 'failed';
+type MetadataValue = string | number | boolean | null | MetadataValue[] | {
+    [key: string]: MetadataValue;
+};
+interface ConvertFileBounds {
+    /** Width in pixels or document units. */
+    w?: number;
+    /** Height in pixels or document units. */
+    h?: number;
+    /** Total page/slide count when available. */
+    total?: number;
+}
+type FileResult = [
+    /** Storage key, relative path, or URL after the backend expands task results. */
+    path: string,
+    /** File size in bytes. */
+    bytes: number,
+    /** MD5 hash. */
+    hash: string
+];
+type ConvertFileResult = [
+    /** Storage key, relative path, or URL after the backend expands task results. */
+    path: string,
+    /** File size in bytes. */
+    bytes: number,
+    /** MD5 hash. */
+    hash: string,
+    /** Optional conversion bounds/page metadata. */
+    bounds?: ConvertFileBounds | null
+];
+interface DeckTask<T extends DeckTaskType = DeckTaskType> {
+    /** Task id generated by the backend. */
+    id: string;
+    /** Space/workspace id owning this task. */
+    spaceId: string;
+    /** Task type. */
+    type: T;
+    /** Current task status. */
+    status: DeckTaskStatus;
+    /** Ordered input file ids. */
+    fileIds?: string[];
+    /** Task display name. */
+    name?: string;
+    /** Task parameters. */
+    params?: DeckTaskTypeParams[T];
+    /** Optional preview payload. */
+    preview?: DeckTaskTypePreview[T];
+    /** Task result payload. Prefer `tasks.down(taskId)` — detail responses no longer include results. */
+    result?: DeckTaskTypeResult[T] | null;
+    /** Error message for failed tasks. */
+    error?: string | null;
+    /** ISO create timestamp. */
+    createdAt?: string;
+    /** ISO update timestamp. */
+    updatedAt?: string;
+}
+interface TaskListResponse<T extends DeckTaskType = DeckTaskType> {
+    /** Tasks in the requested page. */
+    tasks: DeckTask<T>[];
+    /** Total matching task count when the backend returns it. */
+    total?: number;
+}
+interface AuthUuidStorage {
+    get(): string | null | undefined | Promise<string | null | undefined>;
+    set(value: string): void | Promise<void>;
+}
+interface CreateDeckOptions {
+    /** API root address. Defaults to https://app.deckflow.com/v1. */
+    root?: string;
+    /** User auth token sent as X-Auth-Token. */
+    token?: string;
+    /** API key sent as Authorization: Bearer {apiKey}. */
+    apiKey?: string;
+    /** Default space id used by task and upload calls. When omitted, resolved from GET /user. */
+    spaceId?: string;
+    /** Explicit client UUID (UUID v4) sent as X-Auth-UUID. Skips automatic persistence. */
+    authUuid?: string;
+    /** Custom UUID storage for SSR, tests, or embedded integrations. */
+    authUuidStorage?: AuthUuidStorage;
+    /**
+     * Called once after a 401 when a user token is present. Returned token is saved and the request is
+     * retried. If omitted or refresh fails, the request fails unless allowGuestFallback is true.
+     */
+    onUnauthorized?: () => Promise<{
+        token: string;
+        spaceId?: string;
+    } | string>;
+    /** Permit a failed login to fall back to guest. Defaults to true in Node, false in /browser. */
+    allowGuestFallback?: boolean;
+    /** Retry POSTs on transient network/server failures. Defaults to true in Node, false in /browser. */
+    retryMutations?: boolean;
+    /** Called once after a 402 response; request is retried after this resolves. */
+    onPaymentRequired?: () => Promise<void>;
+}
+/** Current user profile returned by GET /user. */
+interface UserSelf {
+    id: string;
+}
+interface CreateTaskParams<T extends DeckTaskType = DeckTaskType> {
+    /** Stops client-side upload/submission; does not cancel an already-created cloud task. */
+    signal?: AbortSignal;
+    /** Space/workspace id. Falls back to createDeck({ spaceId }) or user.self id. */
+    spaceId?: string;
+    /** Ordered input file ids. Required for most tasks except generation/html.buildPlayer. */
+    fileIds?: string[];
+    /**
+     * Local files for the task. When total size is under {@link INLINE_TASK_FILES_MAX_BYTES}
+     * and no `fileIds` are also provided, they are sent inline on task creation as `files`.
+     * Otherwise they are uploaded first and the resulting ids are appended after `fileIds`.
+     */
+    files?: TaskUploadInput[];
+    /** Task type. */
+    type: T;
+    /** Optional task display name. */
+    name?: string;
+    /** Task parameters determined by type. */
+    params?: DeckTaskTypeParams[T];
+    /** Default upload options applied to files. Per-file options override these values. */
+    upload?: TaskUploadOptions;
+}
+interface TaskShortcutParams<T extends DeckTaskType> extends Omit<CreateTaskParams<T>, 'type'> {
+}
+interface ListTasksParams<T extends DeckTaskType = DeckTaskType> {
+    signal?: AbortSignal;
+    /** Space/workspace id. Falls back to createDeck({ spaceId }) or user.self id. */
+    spaceId?: string;
+    /** Optional task type filter. */
+    type?: T;
+    /** Start index for pagination. */
+    startIndex?: number;
+    /** Max results for pagination. */
+    maxResults?: number;
+}
+interface WaitForTaskOptions {
+    /** Space containing this task. Defaults to the client's space. */
+    spaceId?: string;
+    /** Stops waiting; does not cancel the cloud task. */
+    signal?: AbortSignal;
+    /** Timeout in seconds. Defaults to 300. */
+    timeout?: number;
+    /** Use Server-Sent Events first, then fall back to polling. Defaults to true. */
+    useEventStream?: boolean;
+    /** Called whenever a task update is received. */
+    onProgress?: (task: DeckTask) => void;
+    /** Poll interval in milliseconds when polling is used. */
+    pollInterval?: number;
+}
+type TaskDownloadType = 'html' | 'pptx' | 'image';
+interface TaskDownloadOptions {
+    spaceId?: string;
+    signal?: AbortSignal;
+    /** Download target type. Only used by generation and revamp tasks. Defaults to backend value html. */
+    type?: TaskDownloadType;
+}
+interface TaskDownloadUrlResult {
+    /** Signed/download URL generated by the backend. */
+    downloadUrl: string;
+}
+type TaskDownResult<T extends DeckTaskType = DeckTaskType> = T extends 'generation' | 'revamp' | 'translation' ? TaskDownloadUrlResult : DeckTaskTypeResult[T];
+interface SubscribeTaskHandlers<T extends DeckTaskType = DeckTaskType> {
+    spaceId?: string;
+    signal?: AbortSignal;
+    /** Called for each task update. */
+    onUpdate: (task: DeckTask<T>) => void;
+    /** Called when the stream or parser fails. */
+    onError?: (error: Error) => void;
+}
+type UploadPlatform = 'oss' | 'local';
+interface AuthInfo {
+    /** Upload URL. */
+    url: string;
+    /** Headers required by the storage provider. */
+    headers: Record<string, string>;
+    /** Optional Authorization header returned by the backend. */
+    Authorization?: string;
+}
+interface PartAuth {
+    /** Part upload URL. */
+    url: string;
+    /** Headers required by the storage provider. */
+    headers: Record<string, string>;
+    /** Optional Authorization header returned by the backend. */
+    Authorization?: string;
+}
+interface UploadAuthResponse {
+    /** File id to use in task fileIds. */
+    id: string;
+    /** Storage key. */
+    key: string;
+    /** File hash. */
+    hash: string;
+    /** Storage provider. */
+    platform: UploadPlatform;
+    /** Whether multipart upload is required. */
+    multipart: boolean;
+    /** Single upload or multipart completion auth. */
+    auth?: AuthInfo;
+    /** Multipart upload id. */
+    multipartUploadId?: string;
+    /** Multipart part size in bytes. */
+    multipartPartSize?: number;
+    /** Multipart part upload auth list. */
+    multipartPartAuths?: PartAuth[];
+}
+interface PartResult {
+    /** 1-based part number. */
+    partNumber: number;
+    /** ETag returned by OSS-style storage. */
+    eTag?: string;
+    /** Hash returned by local storage. */
+    hash?: string;
+}
+interface RequestUploadParams {
+    signal?: AbortSignal;
+    /** Space/workspace id. Falls back to createDeck({ spaceId }). */
+    spaceId?: string;
+    /** File name. */
+    name: string;
+    /** File byte size. */
+    bytes: number;
+    /** File MD5 hash. */
+    hash: string;
+    /** Desired chunk size in bytes. */
+    chunkSize?: number;
+}
+type UploadInput = string | Uint8Array | ArrayBuffer | Blob;
+interface UploadOptions {
+    signal?: AbortSignal;
+    /** Space/workspace id. Falls back to createDeck({ spaceId }). */
+    spaceId?: string;
+    /** File name. Required for binary inputs that do not carry a name. */
+    name?: string;
+    /** File MD5 hash. Calculated automatically when omitted. */
+    hash?: string;
+    /** Desired chunk size in bytes. */
+    chunkSize?: number;
+    /** Upload progress from 0 to 1. */
+    onProgress?: (percentage: number) => void;
+}
+type TaskUploadOptions = Omit<UploadOptions, 'spaceId'>;
+type TaskUploadInput = UploadInput | ({
+    /** File-like value, Node.js path, or binary data to upload. Paths are Node.js-only. */
+    input: UploadInput;
+} & TaskUploadOptions);
+/** Normalized local file ready for inline task creation or async upload. */
+interface PreparedUpload {
+    name: string;
+    bytes: number;
+    hash: string;
+    data: Uint8Array | Blob;
+    chunkSize: number;
+}
+interface FileUploadResult {
+    /** File id to use in task fileIds. */
+    id: string;
+    /** File name. */
+    name: string;
+    /** Storage key. */
+    key?: string;
+    /** File byte size. */
+    bytes: number;
+    /** File MD5 hash. */
+    hash: string;
+}
+interface FileCompressParams {
+    /** Optional task id passed through to the compressor logs. */
+    id?: string;
+}
+type OcrLanguage = 'ar' | 'bg' | 'cs' | 'da' | 'de' | 'el' | 'en' | 'es' | 'et' | 'fi' | 'fr' | 'hu' | 'id' | 'it' | 'ja' | 'ko' | 'lt' | 'lv' | 'nb' | 'nl' | 'pl' | 'pt' | 'ro' | 'ru' | 'sk' | 'sl' | 'sv' | 'tr' | 'uk' | 'zh-hans' | 'zh-hant' | 'ara' | 'bul' | 'ces' | 'dan' | 'deu' | 'grek' | 'eng' | 'spa' | 'est' | 'fin' | 'fra' | 'hun' | 'ind' | 'ita' | 'jpn' | 'kor' | 'lit' | 'lav' | 'nor' | 'nld' | 'pol' | 'por' | 'ron' | 'rus' | 'slk' | 'slv' | 'swe' | 'tur' | 'ukr' | 'chi_sim' | 'chi_tra';
+interface ImageOcrParams {
+    /** OCR language code. */
+    language: OcrLanguage;
+}
+interface PptxSplitParams {
+    /** Slide indexes to keep, 1-based. If omitted, every slide is split. */
+    indexes?: number[];
+    /** Allowed slide size as [width, height]. */
+    allowSize?: [width: number, height: number];
+    /** Expected PPT id to write. */
+    pptId?: string;
+    /** Expected version ids to write. Must match slide count or selected indexes. */
+    versionIds?: string[];
+    /** Replacement file pairs as [relativePath, keyPath]. */
+    replaceFiles?: [relativePath: string, keyPath: string][];
+}
+interface PptxContactId {
+    /** PPT id. */
+    pptId: string;
+    /** Branch id. */
+    branchId: string;
+}
+interface PptxSectionItem {
+    /** Section name. */
+    name: string;
+    /** Number of slides in this section. */
+    count: number;
+}
+interface PptxJoinParams {
+    /** 1-based slide indexes to hide. */
+    hiddens?: number[];
+    /** Contact id to preserve or write. */
+    contactId?: PptxContactId;
+    /** Section metadata. */
+    sections?: PptxSectionItem[];
+    /** Changed layout map. */
+    changedLayoutMap?: [sourceSlideIndex: number, targetSlideIndex: number][];
+    /** Whether to auto resize slides. */
+    autoResize?: boolean;
+}
+type PptxGetFontInfoParams = Record<never, never>;
+interface PptxGetTextShapesParams {
+    /** Whether returned shapes include child shapes. */
+    hasChildren?: boolean;
+    /** Page numbers to inspect. */
+    pickPages?: number[];
+    /** Image filter by area ratio from 0 to 1. */
+    imageFilter?: number;
+    /** Whether duplicate shape ids should be fixed. */
+    needFixDuplicatedShapeId?: boolean;
+    /** Include layout shapes. */
+    includeLayout?: boolean;
+    /** Include master shapes. */
+    includeMaster?: boolean;
+    /** Include speaker notes. */
+    includeNotes?: boolean;
+    /** Ignore empty text shapes. */
+    ignoreEmptyText?: boolean;
+    /** Return only brief shape information. */
+    onlyBrief?: boolean;
+}
+interface PptxEmbedFontsParams {
+    /** Font family names to embed into the PPTX. */
+    usedFonts: string[];
+}
+interface ConvertPptToImageParams {
+    /** Output image width/resolution. */
+    resolution?: 1080 | 1920 | 2560;
+    /** Output image format. */
+    format?: 'png' | 'jpg';
+}
+type ConvertPptToPptxParams = Record<never, never>;
+type ConvertPptToPdfParams = Record<never, never>;
+type ConvertDocToPdfParams = Record<never, never>;
+type ConvertPptToVideoParams = Record<never, never>;
+interface ConvertPdfToImageParams {
+    /** Long-edge max resolution. Defaults to backend value 1080. */
+    resolution?: number;
+    /** Output image format. */
+    format?: 'png' | 'jpg';
+}
+type ConvertKeynoteToImageParams = Record<never, never>;
+type ConvertKeynoteToPdfParams = Record<never, never>;
+interface ConvertKeynoteToHtmlParams {
+    /** Whether the backend should create a hosted HTML path. The task API currently overrides this to false. */
+    isHost?: boolean;
+}
+interface ConvertHtmlToPngParams {
+    /** Render scale. */
+    scale?: number;
+    /** Capture the full page. */
+    fullPage?: boolean;
+    /** Inline HTML content. Ignored when fileIds are used. */
+    html?: string;
+    /** Viewport width. */
+    width?: number;
+    /** Viewport height. */
+    height?: number;
+}
+interface ConvertMarkdownToPngParams {
+    /** Render scale. */
+    scale?: number;
+    /** Theme name. */
+    theme?: 'light' | 'dark';
+    /** Page width. */
+    pageWidth?: number;
+    /** Inline markdown content. Ignored when fileIds are used. */
+    markdown?: string;
+}
+interface ConvertHtmlToPptxParams {
+    /** Whether to embed fonts in the output PPTX. */
+    needEmbedFonts?: boolean;
+    /** Inline HTML content. Ignored when fileIds are used. */
+    html?: string;
+    /** Slide width. */
+    width?: number;
+    /** Slide height. */
+    height?: number;
+}
+type VideoCompressParams = Record<never, never>;
+type ImageConvertWebpParams = Record<never, never>;
+interface HtmlBuildPlayerContent {
+    /** OSS/storage key for this content page. */
+    key?: string;
+    /** Inline HTML for this content page. */
+    html?: string;
+}
+interface HtmlBuildPlayerParams {
+    /** HTML pages/segments. Each item should provide either key or html. */
+    contents: HtmlBuildPlayerContent[];
+    /** Player page width. */
+    pageWidth: number;
+    /** Player page height. */
+    pageHeight: number;
+    /** Player title. */
+    title: string;
+    /** Player description. */
+    description: string;
+    /** Brand mark position. */
+    brandMarkPosition: 'none' | 'leftTop' | 'leftBottom' | 'rightTop' | 'rightBottom';
+}
+interface ImageResizeParams {
+    /** Maximum width. Either maxWidth or maxHeight is required by the backend. */
+    maxWidth?: number;
+    /** Maximum height. Either maxWidth or maxHeight is required by the backend. */
+    maxHeight?: number;
+    /** Optional target storage path. */
+    target?: string;
+}
+interface GenerationParams {
+    /** User input text. */
+    inputText?: string;
+    /** Whether to enable search. */
+    enableSearch?: boolean;
+    /** Whether to use an advanced model. */
+    advancedModel?: boolean;
+    /** Whether to enable fast mode. */
+    fastMode?: boolean;
+    /** Content generation intent. */
+    intent?: string;
+    /** Target audience. */
+    audience?: string;
+    /** Expected page count. */
+    pageCount?: number;
+    /** Document author. */
+    author?: string;
+}
+interface TranslationParams {
+    /** Translation model. */
+    model: 'Standard' | 'Pro';
+    /** Source language. */
+    from: string;
+    /** Target language. */
+    to: string;
+    /** Whether to use glossary. */
+    useGlossary: boolean;
+    /** Whether to translate images inside the document. */
+    imageTranslate: boolean;
+}
+interface RevampParams {
+    /** Output language. */
+    lang?: string;
+}
+interface PptxSlideResult {
+    /** File MD5 hash. */
+    hash: string;
+    /** Storage path or URL. */
+    path: string;
+    /** File size in bytes. */
+    bytes: number;
+    /** Whether the slide was hidden. */
+    hidden?: boolean;
+    /** Snapshot image path when available. */
+    snapImage?: string;
+    /** Speaker notes text. */
+    notes?: string;
+    /** Version id written to the split slide. */
+    versionId?: string;
+}
+interface PptxSplitResult {
+    /** Source PPT metadata. */
+    ppt: {
+        /** Source slide size as [width, height]. */
+        size: [width: number, height: number];
+    };
+    /** Section metadata read from source file. */
+    sections: PptxSectionItem[];
+    /** Split slide files. */
+    slides: PptxSlideResult[];
+}
+interface PptxFontInfo {
+    /** Font names used by the document. */
+    fonts: string[];
+    /** Whether the document contains embedded fonts. */
+    embeddedFont: boolean;
+    /** Whether embedded fonts are subsets. */
+    subsetFont: boolean;
+}
+interface PptxShapeFile {
+    /** Storage path or URL. */
+    path?: string;
+    /** File extension including dot. */
+    ext?: string;
+    /** Description, commonly the original file name. */
+    desc?: string;
+    /** File size in bytes. */
+    bytes?: number;
+    /** MD5 hash. */
+    hash?: string;
+}
+interface PptxTextRun {
+    /** Text content. */
+    text: string;
+    /** Font family. */
+    font?: string;
+    /** Font size. */
+    fontSize?: number;
+    /** Text color. */
+    textColor?: string;
+    /** Text opacity/alpha. */
+    textAlpha?: number;
+    /** Bold marker from PPT XML. */
+    fontBold?: string;
+    /** Italic marker from PPT XML. */
+    fontItalic?: string;
+    /** Underline marker from PPT XML. */
+    fontUnderline?: string;
+    /** Strike marker from PPT XML. */
+    fontStrike?: string;
+    /** Character spacing. */
+    charSpacing?: number;
+}
+interface PptxTextParagraph {
+    /** Horizontal alignment. */
+    horizontalAlignment?: string;
+    /** Whether text reads right-to-left. */
+    rtl?: '0' | '1';
+    /** Text runs in this paragraph. */
+    children: PptxTextRun[];
+}
+interface PptxShapeContent {
+    /** Plain text content. */
+    text?: string;
+    /** Rich text paragraphs. */
+    textNode?: PptxTextParagraph[];
+    /** Picture file info. */
+    picture?: PptxShapeFile & {
+        crop?: {
+            left?: number;
+            top?: number;
+            right?: number;
+            bottom?: number;
+            width?: number;
+            height?: number;
+            offX?: number;
+            offY?: number;
+        };
+        alpha?: number;
+    };
+    /** Chart info. */
+    chart?: {
+        rId?: string;
+        type: 'bar' | 'line' | 'pie';
+    };
+    /** Video file info. */
+    video?: PptxShapeFile & {
+        thumbnail?: PptxShapeFile;
+    };
+    /** Audio file info. */
+    audio?: PptxShapeFile & {
+        thumbnail?: PptxShapeFile;
+    };
+}
+interface PptxShape {
+    /** Shape id. */
+    id: number;
+    /** Shape name. */
+    name: string;
+    /** Shape category. */
+    type: 'shape' | 'picture' | 'group' | 'connectionShape' | 'graphicFrame';
+    /** Placeholder type. */
+    phType?: string;
+    /** Whether the shape is a text box. */
+    txBox?: boolean;
+    /** Whether the shape is hidden. */
+    hidden?: boolean;
+    /** Preset geometry name. */
+    shape?: string;
+    /** Shape path in the slide tree. */
+    path?: string;
+    /** Width. */
+    width: number;
+    /** Height. */
+    height: number;
+    /** Left offset. */
+    left: number;
+    /** Top offset. */
+    top: number;
+    /** Rotation. */
+    rotation?: number;
+    /** Vertical flip. */
+    flipV?: boolean;
+    /** Horizontal flip. */
+    flipH?: boolean;
+    /** Font family. */
+    font?: string;
+    /** Font size. */
+    fontSize?: number;
+    /** Text color. */
+    textColor?: string;
+    /** Fill color. */
+    color?: string;
+    /** Children for group shapes. */
+    children?: PptxShape[];
+    /** Shape content. */
+    content?: PptxShapeContent;
+}
+interface PptxPageInfo {
+    /** Page type. */
+    type: 'slide' | 'master' | 'layout';
+    /** Page file path. */
+    file: string;
+    /** Layout file path for slide pages. */
+    layoutFile?: string;
+    /** Master file path for slide/layout pages. */
+    masterFile?: string;
+    /** Shape tree. */
+    spTree: PptxShape[];
+    /** Speaker notes. */
+    notes?: string[];
+    /** Embedded text entries. */
+    embedText?: {
+        text: string;
+        fontInfo?: PptxFontInfo;
+    }[];
+    /** Color mapping. */
+    colorMap?: Record<string, string>;
+}
+interface PptxGetTextShapesResult {
+    /** Number of pages. */
+    pageNum: number;
+    /** Slide width. */
+    width: number;
+    /** Slide height. */
+    height: number;
+    /** Slide page info. */
+    slides: PptxPageInfo[];
+    /** Layout page info when requested. */
+    layouts?: PptxPageInfo[];
+    /** Master page info when requested. */
+    masters?: PptxPageInfo[];
+    /** Picked PPTX path when duplicate shape ids were fixed. */
+    pickedPptx?: string;
+    /** Extracted image map keyed by original relative path. */
+    images?: Record<string, FileResult>;
+    /** Whether duplicate shape ids were detected. */
+    hasDuplicatedShapeId?: boolean;
+}
+interface ImageOcrResult {
+    /** Tesseract job id. */
+    jobId?: string;
+    /** OCR data payload. */
+    data: {
+        /** Recognized text. */
+        text: string;
+        /** Mean confidence. */
+        confidence?: number;
+        /** Recognized blocks. */
+        blocks?: MetadataValue[];
+        /** Recognized paragraphs. */
+        paragraphs?: MetadataValue[];
+        /** Recognized lines. */
+        lines?: MetadataValue[];
+        /** Recognized words. */
+        words?: MetadataValue[];
+        /** Recognized symbols. */
+        symbols?: MetadataValue[];
+        [key: string]: MetadataValue | undefined;
+    };
+}
+interface HtmlToPptxResult {
+    /** Output PPTX file. */
+    target: FileResult;
+    /** Fonts used while converting HTML to PPTX. */
+    usedFonts: string[];
+}
+
+/** `pptx.parse` 的结果：PPTX 对象模型 */
+type PptxTaskParseResult = PptxParseResult;
+interface DeckTaskTypeParams {
+    'file.compress': FileCompressParams;
+    'image.ocr': ImageOcrParams;
+    'pptx.split': PptxSplitParams;
+    'pptx.join': PptxJoinParams;
+    'pptx.getFontInfo': PptxGetFontInfoParams;
+    'pptx.getTextShapes': PptxGetTextShapesParams;
+    'pptx.embedFonts': PptxEmbedFontsParams;
+    'convertor.ppt2image': ConvertPptToImageParams;
+    'convertor.ppt2pptx': ConvertPptToPptxParams;
+    'convertor.ppt2pdf': ConvertPptToPdfParams;
+    'convertor.doc2pdf': ConvertDocToPdfParams;
+    'convertor.ppt2video': ConvertPptToVideoParams;
+    'convertor.pdf2image': ConvertPdfToImageParams;
+    'convertor.keynote2image': ConvertKeynoteToImageParams;
+    'convertor.keynote2html': ConvertKeynoteToHtmlParams;
+    'convertor.keynote2pdf': ConvertKeynoteToPdfParams;
+    'convertor.html2png': ConvertHtmlToPngParams;
+    'convertor.markdown2png': ConvertMarkdownToPngParams;
+    'convertor.html2pptx': ConvertHtmlToPptxParams;
+    'html.buildPlayer': HtmlBuildPlayerParams;
+    'video.compress': VideoCompressParams;
+    'image.convertWebp': ImageConvertWebpParams;
+    'image.resize': ImageResizeParams;
+    'pdf.pdfParse': PdfParseTaskParams;
+    'pptx.parse': PptxParseTaskParams;
+    'docx.parseTextAndImage': DocxParseTaskParams;
+    'keynote.parseTextAndImage': KeynoteParseTaskParams;
+    'html.getByURL': HtmlGetByUrlTaskParams;
+    'parse.convert': ConvertTaskParams;
+    generation: GenerationParams;
+    translation: TranslationParams;
+    revamp: RevampParams;
+}
+interface DeckTaskTypeResult {
+    'file.compress': ConvertFileResult;
+    'image.ocr': ImageOcrResult;
+    'pptx.split': PptxSplitResult;
+    'pptx.join': FileResult;
+    'pptx.getFontInfo': PptxFontInfo;
+    'pptx.getTextShapes': PptxGetTextShapesResult;
+    'pptx.embedFonts': FileResult;
+    'convertor.ppt2image': ConvertFileResult[];
+    'convertor.ppt2pptx': ConvertFileResult[];
+    'convertor.ppt2pdf': ConvertFileResult[];
+    'convertor.doc2pdf': ConvertFileResult[];
+    'convertor.ppt2video': ConvertFileResult[];
+    'convertor.pdf2image': ConvertFileResult[];
+    'convertor.keynote2image': ConvertFileResult[];
+    'convertor.keynote2html': ConvertFileResult[];
+    'convertor.keynote2pdf': ConvertFileResult[];
+    'convertor.html2png': FileResult;
+    'convertor.markdown2png': FileResult;
+    'convertor.html2pptx': HtmlToPptxResult;
+    'html.buildPlayer': FileResult;
+    'video.compress': FileResult;
+    'image.convertWebp': FileResult;
+    'image.resize': FileResult;
+    'pdf.pdfParse': PdfParseResult;
+    'pptx.parse': PptxParseResult;
+    'docx.parseTextAndImage': DocxParseResult;
+    'keynote.parseTextAndImage': KeynoteParseResult;
+    'html.getByURL': HtmlGetByUrlResult;
+    'parse.convert': ConvertTaskResult;
+    generation: Required<{
+        image?: FileResult;
+        pptx?: FileResult;
+        html?: FileResult;
+    }>;
+    translation: FileResult;
+    revamp: Required<{
+        image?: FileResult;
+        pptx?: FileResult;
+        html?: FileResult;
+    }>;
+}
+interface DeckTaskTypePreview {
+    'file.compress': {
+        bytes: number;
+    };
+    'pptx.split': {
+        fileNumber: number;
+    };
+    'pptx.join': {
+        bytes: number;
+    };
+    'convertor.html2pptx': FileResult[];
+    'video.compress': {
+        bytes: number;
+    };
+    'image.resize': {
+        bytes: number;
+    };
+    generation: {
+        previewURL?: string;
+    };
+    translation: {
+        previewURL?: string;
+        before: FileResult[];
+        after: FileResult[];
+    };
+    revamp: {
+        previewURL?: string;
+        before: FileResult[];
+    };
+    'image.ocr': never;
+    'pptx.getFontInfo': never;
+    'pptx.getTextShapes': never;
+    'pptx.embedFonts': never;
+    'convertor.ppt2image': never;
+    'convertor.ppt2pptx': never;
+    'convertor.ppt2pdf': never;
+    'convertor.doc2pdf': never;
+    'convertor.ppt2video': never;
+    'convertor.pdf2image': never;
+    'convertor.keynote2image': never;
+    'convertor.keynote2html': never;
+    'convertor.keynote2pdf': never;
+    'convertor.html2png': never;
+    'convertor.markdown2png': never;
+    'html.buildPlayer': never;
+    'image.convertWebp': never;
+    'pdf.pdfParse': never;
+    'parse.convert': never;
+    'pptx.parse': never;
+    'docx.parseTextAndImage': never;
+    'keynote.parseTextAndImage': never;
+    'html.getByURL': never;
+}
+
+/**
+ * 解析类任务的参数与结果类型。
+ *
+ * 这些结构由服务端的 slave 解析器产出，此处**照抄而非引用** `@deckflow/platform-slave` ——
+ * 公开 SDK 不能依赖私有 registry 上的服务端包。字段随服务端演进时需同步更新。
+ *
+ * **parse 只出 IR，markdown 由 `parse.convert` 从已存储的 IR 派生。** 解析结果里不再有
+ * 任何 markdown 字段：View 是按需派生的，不是解析的副产品。
+ *
+ * 注意：任务结果经服务端 `key2url` 处理后，OSS key 字段已被展开为可访问 URL —— 唯独
+ * `irKey` 例外，它是引用不是产物，原样保留（展开成签名地址会让引用几小时后失效，而 IR
+ * 承诺活 7 天）。
+ */
+
+/** 支持解析的格式，与 IR 信封的 `format` 一一对应。 */
+type IrFormat = 'pdf' | 'pptx' | 'docx' | 'keynote' | 'html';
+/**
+ * 每个 parse 任务结果都带的两个字段，指向这次解析存下来的 IR。
+ *
+ * 拿着 `irKey` 就能在保留期内反复调 `deck.convert()` 派生不同 View，源文件不必再传一次。
+ */
+interface IrResult {
+    /** 已存储 IR 的 key */
+    irKey: string;
+    /** 该 IR 的版本标签，convert 的门禁按它判断认不认 */
+    irSchemaVersion: string;
+}
+/** IR 保留期：7 天。过期后引用会得到 `irExpired`，重新 parse 即可。 */
+declare const IR_RETENTION_DAYS = 7;
+/** IR 中被引用的二进制资源。**只存持久 key，不存带效期的地址。** */
+interface IrResource {
+    /** body 里引用这份资源用的标识：pdf 用工件内相对路径，pptx 用 zip 内路径，其余即 key */
+    ref: string;
+    key: string;
+    bytes: number;
+    hash: string;
+    /** 建议的落盘相对路径，形如 `assets/p1_i0000.png` */
+    suggestedPath: string;
+}
+/**
+ * IR 信封：自描述信头 + 解析器原样输出。
+ *
+ * 这是 IR 的**存储形态**。parse 任务的响应仍是扁平的（`slides` / `content` / `document`
+ * 直接在顶层），信封只在存储里出现，`deck.convert()` 按 `irKey` 读它。
+ */
+interface IrEnvelope<B = unknown> {
+    format: IrFormat;
+    schemaVersion: string;
+    producer: {
+        name: string;
+        version: string;
+    };
+    source: {
+        sha256?: string;
+        name?: string;
+        bytes?: number;
+    };
+    /** ISO 8601 */
+    createdAt: string;
+    body: B;
+    resources?: IrResource[];
+}
+/** convert 支持的 View 目标。v1 只有 markdown，枚举位是给后续 html / text 预留的。 */
+type ConvertTarget = 'markdown';
+/** 分页 markdown 的页分隔符，`markdown.split(PAGE_SEPARATOR)` 可还原分页。 */
+declare const PAGE_SEPARATOR = "\n\n---\n\n";
+/**
+ * convert 交付的图片清单，四种格式同一形状。
+ *
+ * 下游据此把 markdown 里的图片下载到本地并改写为相对路径 —— 因为正文里的地址带有效期，
+ * 直接存盘几小时后就是死链。
+ */
+interface ConvertImage {
+    /** 该图片在 markdown 正文里出现的引用，即现签的访问地址 */
+    ref: string;
+    /** OSS 持久 key */
+    key: string;
+    /** 建议落盘相对路径 */
+    suggestedPath: string;
+    bytes?: number;
+    hash?: string;
+}
+interface ConvertTaskParams {
+    /** 已存储 IR 的 key，与 `taskId` 二选一 */
+    irKey?: string;
+    /** 产出该 IR 的 parse 任务 id，与 `irKey` 二选一 */
+    taskId?: string;
+    /** 目标 View，默认 markdown */
+    to?: ConvertTarget;
+    /** pdf：markdown 是否写入逐元素溯源注释，默认 false（注释体积通常是正文的数倍） */
+    markdownMeta?: boolean;
+    /** 分页格式（pptx / keynote）：是否额外返回逐页数组 */
+    markdownPages?: boolean;
+    /** 渲染失败时抛错；默认 false，容错返回 `markdownError` */
+    markdownStrict?: boolean;
+}
+interface ConvertTaskResult {
+    /** IR 的格式，由信封给出而不是调用方声明 */
+    format: IrFormat;
+    schemaVersion: string;
+    to: ConvertTarget;
+    /** 完整 markdown；分页格式按页用 `PAGE_SEPARATOR` 连接 */
+    markdown: string;
+    /** 逐页 markdown，仅 `markdownPages: true` 且格式分页时返回 */
+    markdownPages?: string[];
+    /** 容错模式下渲染失败的原因；有它就说明 markdown 不可信 */
+    markdownError?: string;
+    images: ConvertImage[];
+}
+/** 图片在文档中的位置 */
+interface ParseLocator {
+    /** 所在页，从 0 开始 */
+    pageIndex: number;
+}
+/** 精度/成本档位 */
+type PdfParseProfile = 'fast' | 'balanced' | 'quality';
+/** `[x0, y0, x1, y1]`，单位 pt，左上原点 */
+type PdfBbox = [number, number, number, number];
+/**
+ * 图片落盘后的持久标识。
+ *
+ * **只有 key，没有访问地址**：这份标识写在 IR 里，而 IR 要活 7 天，签名地址活不了那么久。
+ */
+interface StoredPdfAsset {
+    /** 持久化 key，长期标识用它 */
+    key: string;
+    bytes: number;
+    hash: string;
+}
+/** 落盘后的图片；`assetPath` 与 IR 中 `figure.assetPath` 关联 */
+interface ParsedPdfAsset extends StoredPdfAsset {
+    /** 工件内相对路径，形如 `assets/p1_i0000.png` */
+    assetPath: string;
+    /** 现签的访问地址；只在响应里出现，不进 IR */
+    accessURL: string;
+}
+/**
+ * IR 元素的公共形状。
+ *
+ * 这是上游 `@deckflow/pdf-parse` `Element` 的**宽松镜像**：只把常用字段写成强类型，
+ * 其余由索引签名兜住。需要完整判别联合（`heading.level`、`table.table` 等）的调用方
+ * 自行按 `type` 收窄即可，运行时字段就在那里。
+ */
+interface PdfElement {
+    id: string;
+    page: number;
+    order: number;
+    bbox: PdfBbox;
+    parentId: string | null;
+    /** unknown / heading / paragraph / list_item / table / figure / chart / caption / … */
+    type: string;
+    text?: string;
+    /** figure 与 chart 共用；落盘后就地带上 key/bytes/hash */
+    figure?: {
+        assetPath: string | null;
+        kind: 'raster' | 'vector';
+    } & Partial<StoredPdfAsset>;
+    [field: string]: unknown;
+}
+/** 文档元信息 */
+interface PdfDocInfo {
+    status: 'read' | 'unavailable';
+    title: string | null;
+    author: string | null;
+    subject: string | null;
+    keywords: string | null;
+    creator: string | null;
+    producer: string | null;
+    createdAt: string | null;
+    modifiedAt: string | null;
+    lang: string | null;
+}
+/** IR 顶层形状 */
+interface PdfDocument {
+    version: string;
+    schemaVersion: string;
+    source: {
+        sha256: string;
+        pages: number;
+        encrypted: boolean;
+        path?: string;
+    };
+    profile: PdfParseProfile;
+    docInfo: PdfDocInfo;
+    outline: unknown[] | null;
+    pages: unknown[];
+    elements: PdfElement[];
+    furniture?: PdfElement[];
+    annotations: unknown[];
+    warnings: unknown[];
+    stats: Record<string, unknown>;
+    [field: string]: unknown;
+}
+interface PdfParseTaskParams {
+    /** 加密文档的打开口令 */
+    password?: string;
+    /** 精度/成本档位，默认 balanced */
+    parseProfile?: PdfParseProfile;
+    /** 是否抽取图片并落盘，默认 true */
+    includeImages?: boolean;
+}
+/** `pdf.pdfParse` 的结果 */
+interface PdfParseResult extends IrResult {
+    /** 结构化文档模型，figure/chart 已带落盘后的持久 key */
+    document: PdfDocument;
+    /** 落盘图片的平铺索引，省去遍历元素树 */
+    images: ParsedPdfAsset[];
+}
+interface KeynoteTextItem {
+    id: string;
+    text?: string;
+}
+interface KeynoteTableItem {
+    id: string;
+    data: string[];
+}
+interface KeynoteChartItem {
+    id: string;
+    data: {
+        rowName: string[];
+        columnName: string[];
+    };
+}
+interface KeynoteImageItem {
+    id: string;
+    fileName: string;
+    /** 图片所在页，对应 slides 数组索引 */
+    pageIndex?: number;
+    key: string;
+}
+interface KeynoteParseTaskParams {
+    /** 图片区域保留率 0-1，默认 0.05 */
+    stayImageAreaRate?: number;
+}
+interface KeynoteParseResult extends IrResult {
+    pageNum: number;
+    width: number;
+    height: number;
+    slides: {
+        text: KeynoteTextItem[];
+        table: KeynoteTableItem[];
+        chart: KeynoteChartItem[];
+    }[];
+    images?: KeynoteImageItem[];
+}
+/** 段落样式，由 `@deckflow/docx` >= 1.4.0 提供 */
+interface DocxTextStyle {
+    /** 段落样式 id，如 "Heading1"、"1"、"Title" */
+    styleId?: string;
+    /** 段落样式名，已小写，如 "heading 1"、"title" */
+    styleName?: string;
+    /** 大纲级别 0-8，0 为最高级（对应 heading 1） */
+    outlineLvl?: number;
+    /** 首个文本 run 的字号（磅） */
+    fontSize?: number;
+    bold?: boolean;
+    italic?: boolean;
+}
+interface DocxText {
+    idx: number;
+    type: 'text';
+    text: string;
+    style?: DocxTextStyle;
+}
+interface DocxImage {
+    idx: number;
+    type: 'image';
+    /** slave 已把图片二进制转存为 OSS key（经 key2url 后是 URL） */
+    image: string;
+    name: string;
+    hash?: string;
+    bytes?: number;
+}
+interface DocxTableCell {
+    idx: number;
+    type: 'table-cell';
+    children: DocxText[];
+}
+interface DocxTableRow {
+    idx: number;
+    type: 'table-row';
+    children: DocxTableCell[];
+}
+interface DocxTable {
+    idx: number;
+    type: 'table';
+    table: DocxTableRow[];
+}
+interface DocxChart {
+    idx: number;
+    type: 'chart';
+    series: string[];
+    categories: string[];
+}
+interface DocxDiagram {
+    idx: number;
+    type: 'diagram';
+    texts: {
+        idx: number;
+        aps: {
+            idx: number;
+            text: string;
+        }[];
+    }[];
+}
+interface DocxWsp {
+    idx: number;
+    type: 'wsp';
+    children: DocxText[];
+}
+interface DocxGroup {
+    idx: number;
+    type: 'group';
+    children: (DocxWsp | DocxGroup)[];
+}
+interface DocxSdtWpText {
+    idx: number;
+    type: 'sdt-wp';
+    children: DocxText[];
+}
+interface DocxSdt {
+    idx: number;
+    type: 'sdt';
+    children: DocxSdtWpText[];
+}
+type DocxElement = DocxText | DocxImage | DocxTable | DocxChart | DocxDiagram | DocxGroup | DocxWsp | DocxSdt;
+type DocxParseTaskParams = Record<string, never>;
+interface DocxParseResult extends IrResult {
+    width: number;
+    height: number;
+    /** 页数，上游标注为非可靠信息 */
+    pageNum: number;
+    content: DocxElement[];
+}
+/**
+ * `pptx.parse` 的结果：PPTX 对象模型。
+ *
+ * 完整形状见 `@deckflow/presentation` 的 `PresentationAttrs`；这里是**宽松镜像**，
+ * 只固定下游最常用的三个顶层字段，其余由索引签名兜住 —— 公开 SDK 不能依赖
+ * 私有 registry 上的 `@deckflow/presentation`。需要完整类型的调用方自行引它。
+ */
+interface PptxParseResult extends IrResult {
+    slides: Record<string, unknown>[];
+    slideMasters: Record<string, unknown>[];
+    slideSize?: {
+        cx: number;
+        cy: number;
+    };
+    /** zip 内路径 → 落盘后的文件 */
+    files?: Record<string, FileResult>;
+    [field: string]: unknown;
+}
+type PptxParseTaskParams = Record<string, never>;
+interface HtmlGetByUrlTaskParams {
+    /** 目标 http(s) 链接 */
+    url: string;
+    /** 取源码还是运行后的代码，默认 runtime */
+    mode?: 'source' | 'runtime';
+}
+interface HtmlGetByUrlResult extends IrResult {
+    html: string;
+}
+
+/**
+ * 解析类任务的类型与扩展名路由。
+ *
+ * 服务端把 slave 的解析原语原样暴露为 ttask 类型（`pdf.pdfParse` / `pptx.parse` /
+ * `docx.parseTextAndImage` / `keynote.parseTextAndImage` / `html.getByURL`）。
+ *
+ * **它们只产出 IR**：View 由 `parse.convert` 从已存储的 IR 派生，见 `../convert-facade.ts`。
+ */
+
+/** 走解析链路的文档任务类型 */
+type ParseTaskType = 'pdf.pdfParse' | 'pptx.parse' | 'docx.parseTextAndImage' | 'keynote.parseTextAndImage';
+/**
+ * 扩展名 → 任务类型。
+ *
+ * 这是本方案**唯一**外移到客户端的服务端知识，TS / Python / Go SDK 必须保持一致。
+ */
+declare const PARSE_TASK_TYPE_BY_EXTENSION: Record<string, ParseTaskType>;
+/** 支持解析的文件扩展名 */
+declare const PARSE_SUPPORTED_EXTENSIONS: string[];
+/** 支持逐页 markdown（convert 的 `markdownPages`）的任务类型 —— 只有分页格式有 */
+declare const PARSE_PAGED_TASK_TYPES: ReadonlySet<ParseTaskType>;
+/** 从文件名/路径/URL 取小写扩展名（含点），取不到返回空串 */
+declare const extensionOf: (nameOrPath: string) => string;
+/** 按文件名决定该走哪个解析任务类型；不支持的扩展名返回 undefined */
+declare const parseTaskTypeFor: (nameOrPath: string) => ParseTaskType | undefined;
+
+/**
+ * `deck.parse()` 与 `deck.convert()`：两个正交的原语。
+ *
+ * - **parse**：文档 → IR。按扩展名/链接路由到对应解析任务，等完成，取结果。产物是一份
+ *   可以长期持有的 IR，以及指向它的 `irKey`。
+ * - **convert**：IR → View。按 `irKey`（或产出它的 `taskId`）把已存储的 IR 转成指定格式，
+ *   **不重新解析源文件**。
+ *
+ * 这就是 "Parse once, operate repeatedly"：解析一次，在保留期（7 天）内反复转换。
+ *
+ * 想完全自己控参数的调用方走底层即可，不必经过这里：
+ * ```ts
+ * const task = await deck.pptxParse({ files: ['./a.pptx'] });
+ * const ir   = await deck.tasks.down<'pptx.parse'>((await deck.tasks.wait(task.id)).id);
+ * ```
+ */
+
+/** 要解析的文件 */
+interface ParseFileSource {
+    /** 文件：路径（Node）、Blob、或二进制数据 */
+    file: TaskUploadInput;
+    /** 文件名，用于判定扩展名。`file` 是字符串路径时可省略 */
+    name?: string;
+}
+/** 已上传的文件 */
+interface ParseFileIdSource {
+    /** 已上传文件的 id */
+    fileId: string;
+    /** 文件名，用于判定扩展名 */
+    name: string;
+}
+/** 要解析的链接 */
+interface ParseLinkSource {
+    /** http(s) 链接 */
+    url: string;
+    /** 取源码还是运行后的代码，默认 runtime */
+    mode?: 'source' | 'runtime';
+}
+/** 字符串等价于 `{ file }`，用于 Node 下的本地路径 */
+type ParseSource = string | ParseFileSource | ParseFileIdSource | ParseLinkSource;
+interface ParseOptions {
+    /** Stops client-side work; an already-submitted cloud task is not cancelled. */
+    signal?: AbortSignal;
+    /** Invoked immediately after task creation, before waiting. */
+    onTask?: (task: DeckTask) => void;
+    /** Upload progress and file options. */
+    upload?: TaskUploadOptions;
+    /** 空间 id */
+    spaceId?: string;
+    /** 等待任务完成的选项 */
+    wait?: WaitForTaskOptions;
+    /** pdf：加密文档的打开口令 */
+    password?: string;
+    /** pdf：精度/成本档位，默认 balanced */
+    parseProfile?: 'fast' | 'balanced' | 'quality';
+    /** pdf：是否抽取图片并落盘，默认 true */
+    includeImages?: boolean;
+    /** keynote：图片区域保留率 0-1，默认 0.05 */
+    stayImageAreaRate?: number;
+}
+interface ParseResult<R = unknown> extends IrResult {
+    /** 产出该 IR 的任务 id，可交给 `convert()` 或用于回查 */
+    taskId: string;
+    /** 实际使用的任务类型 */
+    type: ParseTaskType | 'html.getByURL';
+    /** IR：服务端结构化结果的原样透传 */
+    ir: R;
+}
+/** convert 的入口引用：给 irKey 或产出它的 taskId，二选一 */
+type ConvertRef = {
+    irKey: string;
+    taskId?: never;
+} | {
+    taskId: string;
+    irKey?: never;
+};
+interface ConvertOptions {
+    signal?: AbortSignal;
+    /** Invoked immediately after task creation, before waiting. */
+    onTask?: (task: DeckTask) => void;
+    /** 目标 View，默认 markdown */
+    to?: ConvertTarget;
+    /** 空间 id */
+    spaceId?: string;
+    /** 等待任务完成的选项 */
+    wait?: WaitForTaskOptions;
+    /** pdf：markdown 是否写入逐元素溯源注释，默认 false */
+    markdownMeta?: boolean;
+    /** 分页格式（pptx / keynote）：是否额外返回逐页数组 */
+    markdownPages?: boolean;
+    /** 渲染失败时抛错；默认 false，容错返回 `markdownError` */
+    markdownStrict?: boolean;
+}
+interface ConvertResult extends ConvertTaskResult {
+    /** 产出该 View 的任务 id，便于回查 */
+    taskId: string;
+}
+
+/** Runtime hooks stay outside the shared HTTP, upload and task implementation. */
+interface DeckRuntime {
+    resolveAuthUuid(options: CreateDeckOptions): Promise<string>;
+    readFile?(path: string, signal?: AbortSignal): Promise<{
+        data: Uint8Array;
+        name: string;
+    }>;
+    useFetchStreams?: boolean;
+}
+
+declare const RETRY_DELAYS_MS: readonly [5000, 10000, 20000];
+declare function setRetryDelaysForTests(delays: readonly number[]): void;
+declare function resetRetryDelaysForTests(): void;
+declare function getRetryDelaysMs(): readonly number[];
+declare function isRetriableHTTPStatus(status?: number): boolean;
+declare function isRetriableAxiosError(error: AxiosError): boolean;
+declare class APIError extends Error {
+    statusCode?: number | undefined;
+    responseData?: MetadataValue | undefined;
+    readonly requestId?: string | undefined;
+    constructor(message: string, statusCode?: number | undefined, responseData?: MetadataValue | undefined, requestId?: string | undefined);
+    static fromAxiosError(error: AxiosError): APIError;
+    static fromResponse(status: number, data: unknown, headers: unknown): APIError;
+    static unauthorizedApiKey(error: AxiosError): APIError;
+    static unauthorizedToken(error: AxiosError): APIError;
+    static paymentRequired(error: AxiosError): APIError;
+}
+/**
+ * Whether an error is worth retrying.
+ *
+ * Retries exist for transient network / upstream issues only.
+ * Explicit application errors (403, 401, 404, 4xx in general) must fail immediately.
+ */
+declare function isRetriableError(error: unknown): boolean;
+
+interface TasksClient {
+    create<T extends DeckTaskType>(params: CreateTaskParams<T>): Promise<DeckTask<T>>;
+    list<T extends DeckTaskType = DeckTaskType>(params?: ListTasksParams<T>): Promise<TaskListResponse<T>>;
+    get<T extends DeckTaskType = DeckTaskType>(taskId: string, options?: {
+        useEventStream?: boolean;
+        signal?: AbortSignal;
+        spaceId?: string;
+    }): Promise<DeckTask<T>>;
+    delete(taskId: string, options?: {
+        signal?: AbortSignal;
+        spaceId?: string;
+    }): Promise<void>;
+    start<T extends DeckTaskType = DeckTaskType>(taskId: string, options?: {
+        signal?: AbortSignal;
+        spaceId?: string;
+    }): Promise<DeckTask<T>>;
+    down<T extends DeckTaskType = DeckTaskType>(taskId: string, options?: TaskDownloadOptions): Promise<TaskDownResult<T>>;
+    wait<T extends DeckTaskType = DeckTaskType>(taskId: string, options?: WaitForTaskOptions): Promise<DeckTask<T>>;
+    subscribe<T extends DeckTaskType = DeckTaskType>(taskId: string, handlers: SubscribeTaskHandlers<T>): Promise<() => void>;
+}
+interface FilesClient {
+    requestUpload(params: RequestUploadParams): Promise<UploadAuthResponse>;
+    prepare(input: UploadInput, options?: UploadOptions): Promise<PreparedUpload>;
+    upload(input: UploadInput, options?: UploadOptions): Promise<FileUploadResult>;
+    uploadPrepared(file: PreparedUpload, options?: UploadOptions): Promise<FileUploadResult>;
+}
+interface DeckClient {
+    /** API root address used by this client. */
+    readonly root: string;
+    /** Task APIs. */
+    readonly tasks: TasksClient;
+    /** Alias for backend ttask APIs. */
+    readonly ttask: TasksClient;
+    /** File upload APIs. */
+    readonly files: FilesClient;
+    /** Update X-Auth-Token for future requests. */
+    setToken(token: string | undefined): void;
+    /** Update Authorization Bearer api key for future requests. */
+    setApiKey(apiKey: string | undefined): void;
+    /** Update default space id for future requests. */
+    setSpaceId(spaceId: string | undefined): void;
+    /** Returns the client UUID sent as X-Auth-UUID on each request. */
+    getAuthUuid(): Promise<string>;
+    fileCompress(params: TaskShortcutParams<'file.compress'>): Promise<DeckTask<'file.compress'>>;
+    imageOcr(params: TaskShortcutParams<'image.ocr'>): Promise<DeckTask<'image.ocr'>>;
+    imageConvertWebp(params: TaskShortcutParams<'image.convertWebp'>): Promise<DeckTask<'image.convertWebp'>>;
+    imageResize(params: TaskShortcutParams<'image.resize'>): Promise<DeckTask<'image.resize'>>;
+    pptxSplit(params: TaskShortcutParams<'pptx.split'>): Promise<DeckTask<'pptx.split'>>;
+    pptxJoin(params: TaskShortcutParams<'pptx.join'>): Promise<DeckTask<'pptx.join'>>;
+    pptxGetFontInfo(params: TaskShortcutParams<'pptx.getFontInfo'>): Promise<DeckTask<'pptx.getFontInfo'>>;
+    pptxGetTextShapes(params: TaskShortcutParams<'pptx.getTextShapes'>): Promise<DeckTask<'pptx.getTextShapes'>>;
+    pptxEmbedFonts(params: TaskShortcutParams<'pptx.embedFonts'>): Promise<DeckTask<'pptx.embedFonts'>>;
+    convertPptToImage(params: TaskShortcutParams<'convertor.ppt2image'>): Promise<DeckTask<'convertor.ppt2image'>>;
+    convertPptToPptx(params: TaskShortcutParams<'convertor.ppt2pptx'>): Promise<DeckTask<'convertor.ppt2pptx'>>;
+    convertPptToPdf(params: TaskShortcutParams<'convertor.ppt2pdf'>): Promise<DeckTask<'convertor.ppt2pdf'>>;
+    convertDocToPdf(params: TaskShortcutParams<'convertor.doc2pdf'>): Promise<DeckTask<'convertor.doc2pdf'>>;
+    convertPptToVideo(params: TaskShortcutParams<'convertor.ppt2video'>): Promise<DeckTask<'convertor.ppt2video'>>;
+    convertPdfToImage(params: TaskShortcutParams<'convertor.pdf2image'>): Promise<DeckTask<'convertor.pdf2image'>>;
+    convertKeynoteToImage(params: TaskShortcutParams<'convertor.keynote2image'>): Promise<DeckTask<'convertor.keynote2image'>>;
+    convertKeynoteToHtml(params: TaskShortcutParams<'convertor.keynote2html'>): Promise<DeckTask<'convertor.keynote2html'>>;
+    convertKeynoteToPdf(params: TaskShortcutParams<'convertor.keynote2pdf'>): Promise<DeckTask<'convertor.keynote2pdf'>>;
+    convertHtmlToPng(params: TaskShortcutParams<'convertor.html2png'>): Promise<DeckTask<'convertor.html2png'>>;
+    convertMarkdownToPng(params: TaskShortcutParams<'convertor.markdown2png'>): Promise<DeckTask<'convertor.markdown2png'>>;
+    convertHtmlToPptx(params: TaskShortcutParams<'convertor.html2pptx'>): Promise<DeckTask<'convertor.html2pptx'>>;
+    htmlBuildPlayer(params: TaskShortcutParams<'html.buildPlayer'>): Promise<DeckTask<'html.buildPlayer'>>;
+    generation(params: TaskShortcutParams<'generation'>): Promise<DeckTask<'generation'>>;
+    translation(params: TaskShortcutParams<'translation'>): Promise<DeckTask<'translation'>>;
+    revamp(params: TaskShortcutParams<'revamp'>): Promise<DeckTask<'revamp'>>;
+    /** 解析类原语：直通任务参数，只产出 IR */
+    pdfParse(params: TaskShortcutParams<'pdf.pdfParse'>): Promise<DeckTask<'pdf.pdfParse'>>;
+    pptxParse(params: TaskShortcutParams<'pptx.parse'>): Promise<DeckTask<'pptx.parse'>>;
+    docxParse(params: TaskShortcutParams<'docx.parseTextAndImage'>): Promise<DeckTask<'docx.parseTextAndImage'>>;
+    keynoteParse(params: TaskShortcutParams<'keynote.parseTextAndImage'>): Promise<DeckTask<'keynote.parseTextAndImage'>>;
+    htmlGetByURL(params: TaskShortcutParams<'html.getByURL'>): Promise<DeckTask<'html.getByURL'>>;
+    convertIr(params: TaskShortcutParams<'parse.convert'>): Promise<DeckTask<'parse.convert'>>;
+    /**
+     * 文档 → IR：按扩展名/链接路由 → 等待 → 取结果。
+     *
+     * 产物里的 `irKey` 可以在保留期（7 天）内反复交给 `convert()`，源文件不必再传。
+     */
+    parse<R = unknown>(source: ParseSource, options?: ParseOptions): Promise<ParseResult<R>>;
+    /**
+     * IR → View：按 `irKey`（或产出它的 `taskId`）转成指定格式，**不重新解析源文件**。
+     */
+    convert(ref: ConvertRef, options?: ConvertOptions): Promise<ConvertResult>;
+}
+declare function createDeckClient(options: CreateDeckOptions, runtime: DeckRuntime): DeckClient;
+
+declare function isValidAuthUuid(value: unknown): value is string;
+declare function generateAuthUuid(): string;
+/** Browser/SSR-safe: no filesystem, environment variables, or import-time storage access. */
+declare function resolveAuthUuid(options?: CreateDeckOptions): Promise<string>;
+
+/** Browser entry point. Auth failures and uncertain task submissions are never silently replayed. */
+declare function createDeck(options?: CreateDeckOptions): DeckClient;
+
+export { APIError, type AuthInfo, type AuthUuidStorage, type ConvertDocToPdfParams, type ConvertFileBounds, type ConvertFileResult, type ConvertHtmlToPngParams, type ConvertHtmlToPptxParams, type ConvertImage, type ConvertKeynoteToHtmlParams, type ConvertKeynoteToImageParams, type ConvertKeynoteToPdfParams, type ConvertMarkdownToPngParams, type ConvertOptions, type ConvertPdfToImageParams, type ConvertPptToImageParams, type ConvertPptToPdfParams, type ConvertPptToPptxParams, type ConvertPptToVideoParams, type ConvertRef, type ConvertResult, type ConvertTarget, type ConvertTaskParams, type ConvertTaskResult, type CreateDeckOptions, type CreateTaskParams, DECK_TASK_TYPES, DEFAULT_CHUNK_SIZE, DEFAULT_POLL_INTERVAL, DEFAULT_ROOT, DEFAULT_TIMEOUT, type DeckClient, type DeckTask, type DeckTaskStatus, type DeckTaskType, type DeckTaskTypeParams, type DeckTaskTypePreview, type DeckTaskTypeResult, type DocxChart, type DocxDiagram, type DocxElement, type DocxGroup, type DocxImage, type DocxParseResult, type DocxParseTaskParams, type DocxSdt, type DocxSdtWpText, type DocxTable, type DocxTableCell, type DocxTableRow, type DocxText, type DocxTextStyle, type DocxWsp, type FileCompressParams, type FileResult, type FileUploadResult, type FilesClient, type GenerationParams, type HtmlBuildPlayerContent, type HtmlBuildPlayerParams, type HtmlGetByUrlResult, type HtmlGetByUrlTaskParams, type HtmlToPptxResult, INLINE_TASK_FILES_MAX_BYTES, IR_RETENTION_DAYS, type ImageConvertWebpParams, type ImageOcrParams, type ImageOcrResult, type ImageResizeParams, type IrEnvelope, type IrFormat, type IrResource, type IrResult, type KeynoteChartItem, type KeynoteImageItem, type KeynoteParseResult, type KeynoteParseTaskParams, type KeynoteTableItem, type KeynoteTextItem, type ListTasksParams, type MetadataValue, type OcrLanguage, PAGE_SEPARATOR, PARSE_PAGED_TASK_TYPES, PARSE_SUPPORTED_EXTENSIONS, PARSE_TASK_TYPE_BY_EXTENSION, type ParseFileIdSource, type ParseFileSource, type ParseLinkSource, type ParseLocator, type ParseOptions, type ParseResult, type ParseSource, type ParseTaskType, type ParsedPdfAsset, type PartAuth, type PartResult, type PdfBbox, type PdfDocInfo, type PdfDocument, type PdfElement, type PdfParseProfile, type PdfParseResult, type PdfParseTaskParams, type PptxContactId, type PptxEmbedFontsParams, type PptxFontInfo, type PptxGetFontInfoParams, type PptxGetTextShapesParams, type PptxGetTextShapesResult, type PptxJoinParams, type PptxPageInfo, type PptxParseResult, type PptxParseTaskParams, type PptxSectionItem, type PptxShape, type PptxShapeContent, type PptxShapeFile, type PptxSlideResult, type PptxSplitParams, type PptxSplitResult, type PptxTaskParseResult, type PptxTextParagraph, type PptxTextRun, type PreparedUpload, RETRY_DELAYS_MS, type RequestUploadParams, type RevampParams, type StoredPdfAsset, type SubscribeTaskHandlers, type TaskDownResult, type TaskDownloadOptions, type TaskDownloadType, type TaskDownloadUrlResult, type TaskListResponse, type TaskShortcutParams, type TaskUploadInput, type TaskUploadOptions, type TasksClient, type TranslationParams, type UploadAuthResponse, type UploadInput, type UploadOptions, type UploadPlatform, type UserSelf, type VideoCompressParams, type WaitForTaskOptions, createDeck, createDeckClient, extensionOf, generateAuthUuid, getRetryDelaysMs, isRetriableAxiosError, isRetriableError, isRetriableHTTPStatus, isValidAuthUuid, parseTaskTypeFor, resetRetryDelaysForTests, resolveAuthUuid, setRetryDelaysForTests };
diff --git a/dist/browser.js b/dist/browser.js
new file mode 100644
index 0000000000000000000000000000000000000000..42c4c4af987384fd812da2c9f116f0857435e72b
--- /dev/null
+++ b/dist/browser.js
@@ -0,0 +1,2108 @@
+// src/files.ts
+import axios from "axios";
+import pLimit from "p-limit";
+
+// src/abort.ts
+function throwIfAborted(signal) {
+  if (signal?.aborted) throw signal.reason ?? new DOMException("Aborted", "AbortError");
+}
+function withSignal(promise, signal) {
+  if (!signal) return promise;
+  return new Promise((resolve, reject) => {
+    const onAbort = () => reject(signal.reason ?? new DOMException("Aborted", "AbortError"));
+    if (signal.aborted) {
+      onAbort();
+      return;
+    }
+    signal.addEventListener("abort", onAbort, { once: true });
+    promise.then(resolve, reject).finally(() => signal.removeEventListener("abort", onAbort));
+  });
+}
+function delay(ms, signal) {
+  return new Promise((resolve, reject) => {
+    if (signal?.aborted) {
+      reject(signal.reason ?? new DOMException("Aborted", "AbortError"));
+      return;
+    }
+    const onAbort = () => {
+      clearTimeout(timer);
+      reject(signal?.reason ?? new DOMException("Aborted", "AbortError"));
+    };
+    const timer = setTimeout(() => {
+      signal?.removeEventListener("abort", onAbort);
+      resolve();
+    }, ms);
+    signal?.addEventListener("abort", onAbort, { once: true });
+  });
+}
+
+// src/types.ts
+var DEFAULT_ROOT = "https://app.deckflow.com/v1";
+var DEFAULT_TIMEOUT = 300;
+var DEFAULT_POLL_INTERVAL = 2e3;
+var DEFAULT_CHUNK_SIZE = 10 * 1024 * 1024;
+var INLINE_TASK_FILES_MAX_BYTES = 10 * 1024 * 1024;
+var DECK_TASK_TYPES = [
+  "file.compress",
+  "image.ocr",
+  "pptx.split",
+  "pptx.join",
+  "pptx.getFontInfo",
+  "pptx.getTextShapes",
+  "pptx.embedFonts",
+  "convertor.ppt2image",
+  "convertor.ppt2pptx",
+  "convertor.ppt2pdf",
+  "convertor.doc2pdf",
+  "convertor.ppt2video",
+  "convertor.pdf2image",
+  "convertor.keynote2image",
+  "convertor.keynote2html",
+  "convertor.keynote2pdf",
+  "convertor.html2png",
+  "convertor.markdown2png",
+  "convertor.html2pptx",
+  "html.buildPlayer",
+  "video.compress",
+  "image.convertWebp",
+  "image.resize",
+  "pdf.pdfParse",
+  "pptx.parse",
+  "docx.parseTextAndImage",
+  "keynote.parseTextAndImage",
+  "html.getByURL",
+  "parse.convert",
+  "generation",
+  "translation",
+  "revamp"
+];
+
+// src/files.ts
+var FilesApi = class {
+  constructor(http, runtime) {
+    this.http = http;
+    this.runtime = runtime;
+  }
+  http;
+  runtime;
+  async requestUpload(params) {
+    throwIfAborted(params.signal);
+    const spaceId = await this.http.resolveSpaceId(params.spaceId, params.signal);
+    if (!spaceId) {
+      throw new Error("spaceId is required for file uploads");
+    }
+    const res = await this.http.post(`/spaces/${encodeURIComponent(spaceId)}/file/auth`, {
+      name: params.name,
+      bytes: params.bytes,
+      hash: params.hash,
+      chunkSize: params.chunkSize ?? DEFAULT_CHUNK_SIZE
+    }, { signal: params.signal });
+    return res.data;
+  }
+  /** Normalize a local input into bytes/name/hash without uploading. */
+  async prepare(input, options = {}) {
+    throwIfAborted(options.signal);
+    const result = await this.normalizeInput(input, options);
+    throwIfAborted(options.signal);
+    return result;
+  }
+  async upload(input, options = {}) {
+    const normalized = await this.prepare(input, options);
+    return await this.uploadPrepared(normalized, options);
+  }
+  async uploadPrepared(file, options = {}) {
+    throwIfAborted(options.signal);
+    try {
+      return await this.performUpload(file, options);
+    } catch (error) {
+      throwIfAborted(options.signal);
+      throw error;
+    }
+  }
+  async performUpload(file, options) {
+    const auth = await this.requestUpload({
+      signal: options.signal,
+      spaceId: options.spaceId,
+      name: file.name,
+      bytes: file.bytes,
+      hash: file.hash,
+      chunkSize: file.chunkSize
+    });
+    throwIfAborted(options.signal);
+    if (!auth.auth) {
+      options.onProgress?.(1);
+      return {
+        id: auth.id,
+        key: auth.key,
+        name: file.name,
+        bytes: file.bytes,
+        hash: file.hash
+      };
+    }
+    if (auth.multipart) {
+      await this.uploadMultipart(file, auth, options.onProgress, options.signal);
+    } else {
+      await this.uploadSingle(file, auth, options.onProgress, options.signal);
+    }
+    throwIfAborted(options.signal);
+    return {
+      id: auth.id,
+      key: auth.key,
+      name: file.name,
+      bytes: file.bytes,
+      hash: file.hash
+    };
+  }
+  async normalizeInput(input, options) {
+    const chunkSize = options.chunkSize ?? DEFAULT_CHUNK_SIZE;
+    if (typeof input === "string") {
+      if (!this.runtime.readFile) {
+        throw new Error("String file paths are only supported in Node.js. Use File, Blob, Uint8Array, or ArrayBuffer in browsers.");
+      }
+      const { data: data2, name: name2 } = await this.runtime.readFile(input, options.signal);
+      const hash = options.hash ?? this.calculateMD5(data2);
+      return {
+        name: options.name ?? name2,
+        bytes: data2.byteLength,
+        hash,
+        data: data2,
+        chunkSize
+      };
+    }
+    if (this.isBlob(input)) {
+      const name2 = options.name ?? input.name;
+      if (!name2) {
+        throw new Error("name is required when uploading a Blob without a name");
+      }
+      const bytes = new Uint8Array(await withSignal(input.arrayBuffer(), options.signal));
+      return {
+        name: name2,
+        bytes: input.size,
+        hash: options.hash ?? this.calculateMD5(bytes),
+        data: input,
+        chunkSize
+      };
+    }
+    const data = input instanceof ArrayBuffer ? new Uint8Array(input) : input;
+    const name = options.name;
+    if (!name) {
+      throw new Error("name is required when uploading binary input");
+    }
+    return {
+      name,
+      bytes: data.byteLength,
+      hash: options.hash ?? this.calculateMD5(data),
+      data,
+      chunkSize
+    };
+  }
+  isBlob(input) {
+    return typeof Blob !== "undefined" && input instanceof Blob;
+  }
+  calculateMD5(data) {
+    const bytes = data instanceof Uint8Array ? data : new Uint8Array(data);
+    const originalLength = bytes.byteLength;
+    const bitLength = originalLength * 8;
+    const paddedLength = ((originalLength + 8 >>> 6) + 1) * 64;
+    const padded = new Uint8Array(paddedLength);
+    padded.set(bytes);
+    padded[originalLength] = 128;
+    const view = new DataView(padded.buffer);
+    view.setUint32(paddedLength - 8, bitLength >>> 0, true);
+    view.setUint32(paddedLength - 4, Math.floor(bitLength / 4294967296), true);
+    let a0 = 1732584193;
+    let b0 = 4023233417;
+    let c0 = 2562383102;
+    let d0 = 271733878;
+    const s = [
+      7,
+      12,
+      17,
+      22,
+      7,
+      12,
+      17,
+      22,
+      7,
+      12,
+      17,
+      22,
+      7,
+      12,
+      17,
+      22,
+      5,
+      9,
+      14,
+      20,
+      5,
+      9,
+      14,
+      20,
+      5,
+      9,
+      14,
+      20,
+      5,
+      9,
+      14,
+      20,
+      4,
+      11,
+      16,
+      23,
+      4,
+      11,
+      16,
+      23,
+      4,
+      11,
+      16,
+      23,
+      4,
+      11,
+      16,
+      23,
+      6,
+      10,
+      15,
+      21,
+      6,
+      10,
+      15,
+      21,
+      6,
+      10,
+      15,
+      21,
+      6,
+      10,
+      15,
+      21
+    ];
+    const k = Array.from({ length: 64 }, (_, i) => Math.floor(Math.abs(Math.sin(i + 1)) * 4294967296));
+    for (let offset = 0; offset < paddedLength; offset += 64) {
+      const m = Array.from({ length: 16 }, (_, i) => view.getUint32(offset + i * 4, true));
+      let a = a0;
+      let b = b0;
+      let c = c0;
+      let d = d0;
+      for (let i = 0; i < 64; i += 1) {
+        let f;
+        let g;
+        if (i < 16) {
+          f = b & c | ~b & d;
+          g = i;
+        } else if (i < 32) {
+          f = d & b | ~d & c;
+          g = (5 * i + 1) % 16;
+        } else if (i < 48) {
+          f = b ^ c ^ d;
+          g = (3 * i + 5) % 16;
+        } else {
+          f = c ^ (b | ~d);
+          g = 7 * i % 16;
+        }
+        const temp = d;
+        const sum = a + f + k[i] + m[g] >>> 0;
+        d = c;
+        c = b;
+        b = b + this.leftRotate(sum, s[i]) >>> 0;
+        a = temp;
+      }
+      a0 = a0 + a >>> 0;
+      b0 = b0 + b >>> 0;
+      c0 = c0 + c >>> 0;
+      d0 = d0 + d >>> 0;
+    }
+    return [a0, b0, c0, d0].map((word) => this.wordToHex(word)).join("");
+  }
+  leftRotate(value, shift) {
+    return (value << shift | value >>> 32 - shift) >>> 0;
+  }
+  wordToHex(word) {
+    return [0, 8, 16, 24].map((shift) => (word >>> shift & 255).toString(16).padStart(2, "0")).join("");
+  }
+  async uploadSingle(file, authResponse, onProgress, signal) {
+    const { auth, platform } = authResponse;
+    if (!auth) {
+      throw new Error("Missing auth in upload response");
+    }
+    const headers = this.authHeaders(auth);
+    if (platform === "oss") {
+      await axios.put(auth.url, file.data, { headers, signal });
+    } else {
+      const { body, headers: formHeaders } = await this.createFormBody(file.name, file.data);
+      await axios.put(auth.url, body, {
+        signal,
+        headers: {
+          ...headers,
+          ...formHeaders
+        }
+      });
+    }
+    throwIfAborted(signal);
+    onProgress?.(1);
+  }
+  async uploadMultipart(file, authResponse, onProgress, signal) {
+    const { auth, multipartPartAuths, multipartPartSize, platform } = authResponse;
+    if (!auth) {
+      throw new Error("Missing auth in upload response");
+    }
+    if (!multipartPartAuths?.length) {
+      throw new Error("Multipart upload authorization missing");
+    }
+    const chunkSize = multipartPartSize ?? file.chunkSize;
+    const partCount = multipartPartAuths.length;
+    const progress = new Array(partCount).fill(0);
+    const updateProgress = () => {
+      onProgress?.(0.95 * progress.reduce((a, b) => a + b, 0) / partCount);
+    };
+    const data = this.isBlob(file.data) ? new Uint8Array(await withSignal(file.data.arrayBuffer(), signal)) : file.data;
+    const limit = pLimit(5);
+    const parts = await Promise.all(
+      multipartPartAuths.map(
+        (partAuth, index) => limit(async () => {
+          throwIfAborted(signal);
+          const result = await this.uploadPart(file.name, data, partAuth, index, chunkSize, platform, signal);
+          progress[index] = 1;
+          updateProgress();
+          return result;
+        })
+      )
+    );
+    parts.sort((a, b) => a.partNumber - b.partNumber);
+    throwIfAborted(signal);
+    await this.completeMultipart(auth, platform, parts, signal);
+    onProgress?.(1);
+  }
+  async uploadPart(name, data, partAuth, partIndex, chunkSize, platform, signal) {
+    const chunk = data.slice(partIndex * chunkSize, (partIndex + 1) * chunkSize);
+    const headers = this.authHeaders(partAuth);
+    if (platform === "oss") {
+      const response2 = await axios.put(partAuth.url, chunk, { headers, signal });
+      let etag = String(response2.headers.etag || "");
+      if (etag.startsWith('"') && etag.endsWith('"')) {
+        etag = etag.slice(1, -1);
+      }
+      return { partNumber: partIndex + 1, eTag: etag };
+    }
+    const { body, headers: formHeaders } = await this.createFormBody(name, chunk);
+    const response = await axios.put(partAuth.url, body, {
+      signal,
+      headers: {
+        ...headers,
+        ...formHeaders
+      }
+    });
+    const responseData = response.data;
+    return { partNumber: partIndex + 1, hash: String(responseData.hash ?? "") };
+  }
+  async completeMultipart(auth, platform, parts, signal) {
+    const headers = this.authHeaders(auth);
+    if (platform === "oss") {
+      const xmlParts = parts.map(
+        (part) => `<Part><PartNumber>${part.partNumber}</PartNumber><ETag>${part.eTag}</ETag></Part>`
+      );
+      await axios.post(auth.url, `<CompleteMultipartUpload>${xmlParts.join("")}</CompleteMultipartUpload>`, {
+        headers,
+        signal
+      });
+      return;
+    }
+    await axios.post(auth.url, { parts }, { headers: { ...headers, "Content-Type": "application/json" }, signal });
+  }
+  authHeaders(auth) {
+    const headers = { ...auth.headers };
+    if (auth.Authorization) {
+      headers.Authorization = auth.Authorization;
+    }
+    return headers;
+  }
+  async createFormBody(name, data) {
+    if (typeof globalThis.FormData === "undefined" || typeof globalThis.Blob === "undefined") {
+      throw new Error("FormData and Blob are required for local uploads in this runtime");
+    }
+    const form = new FormData();
+    const blob = this.isBlob(data) ? data : new Blob([this.toArrayBuffer(data)]);
+    form.append("file", blob, name);
+    return { body: form, headers: {} };
+  }
+  toArrayBuffer(data) {
+    const copy = new ArrayBuffer(data.byteLength);
+    new Uint8Array(copy).set(data);
+    return copy;
+  }
+};
+
+// src/http-client.ts
+import axios2, { AxiosHeaders } from "axios";
+
+// src/errors.ts
+function tryHeaderValue(v) {
+  if (typeof v === "string" && v.trim()) {
+    return v.trim();
+  }
+  if (Array.isArray(v) && typeof v[0] === "string" && v[0].trim()) {
+    return v[0].trim();
+  }
+  return void 0;
+}
+function isRequestIdHeaderName(name) {
+  const n = name.trim().toLowerCase().replace(/_/g, "-");
+  return n === "x-request-id" || n === "x-requestid" || n === "request-id";
+}
+function extractRequestIdFromHeaders(headers) {
+  if (headers == null) {
+    return void 0;
+  }
+  const names = [
+    "x-request-id",
+    "X-Request-Id",
+    "X-RequestId",
+    "x-requestid",
+    "X-RequestID",
+    "request-id",
+    "Request-Id"
+  ];
+  if (typeof headers.get === "function") {
+    const get = headers.get.bind(headers);
+    for (const name of names) {
+      const v = tryHeaderValue(get(name));
+      if (v) {
+        return v;
+      }
+    }
+  }
+  if (typeof headers.toJSON === "function") {
+    const flat = headers.toJSON(true);
+    for (const [key, value] of Object.entries(flat)) {
+      if (isRequestIdHeaderName(key)) {
+        const v = tryHeaderValue(value);
+        if (v) {
+          return v;
+        }
+      }
+    }
+  }
+  if (typeof headers === "object") {
+    for (const [key, value] of Object.entries(headers)) {
+      if (isRequestIdHeaderName(key)) {
+        const v = tryHeaderValue(value);
+        if (v) {
+          return v;
+        }
+      }
+    }
+  }
+  return void 0;
+}
+function extractRequestIdFromBody(data) {
+  if (data == null) {
+    return void 0;
+  }
+  let obj = data;
+  if (typeof data === "string") {
+    try {
+      obj = JSON.parse(data);
+    } catch {
+      return void 0;
+    }
+  }
+  if (typeof obj !== "object" || obj === null) {
+    return void 0;
+  }
+  const d = obj;
+  for (const key of [
+    "requestId",
+    "request_id",
+    "RequestId",
+    "xRequestId",
+    "XRequestId",
+    "traceId",
+    "trace_id",
+    "TraceId",
+    "correlationId",
+    "correlation_id"
+  ]) {
+    const v = d[key];
+    if (typeof v === "string" && v.trim()) {
+      return v.trim();
+    }
+  }
+  return void 0;
+}
+function extractErrorMessage(data, fallback = "Request failed") {
+  if (data == null) {
+    return fallback;
+  }
+  if (typeof data === "string") {
+    return data.length > 500 ? `${data.slice(0, 500)}...` : data;
+  }
+  if (typeof data === "object") {
+    const body = data;
+    const candidate = body.message ?? body.error ?? body.msg;
+    if (typeof candidate === "string" && candidate.trim()) {
+      return candidate.trim();
+    }
+  }
+  return fallback;
+}
+function formatAPIErrorMessage(status, message, requestId) {
+  const base = `API Error (${status ?? "unknown"}): ${message}`;
+  return requestId ? `${base} [X-RequestId: ${requestId}]` : base;
+}
+var RETRY_DELAYS_MS = [5e3, 1e4, 2e4];
+var retryDelaysMs = RETRY_DELAYS_MS;
+function setRetryDelaysForTests(delays) {
+  retryDelaysMs = delays;
+}
+function resetRetryDelaysForTests() {
+  retryDelaysMs = RETRY_DELAYS_MS;
+}
+function getRetryDelaysMs() {
+  return retryDelaysMs;
+}
+function isRetriableHTTPStatus(status) {
+  return status === 604 || status === 502;
+}
+function isRetriableAxiosError(error) {
+  if (!error.response) {
+    return error.code === "ECONNABORTED" || error.code === "ETIMEDOUT" || error.code === "ENOTFOUND" || error.code === "ECONNREFUSED" || error.code === "ERR_NETWORK" || error.message.toLowerCase().includes("network");
+  }
+  return isRetriableHTTPStatus(error.response.status);
+}
+var APIError = class _APIError extends Error {
+  constructor(message, statusCode, responseData, requestId) {
+    super(message);
+    this.statusCode = statusCode;
+    this.responseData = responseData;
+    this.requestId = requestId;
+    this.name = "APIError";
+  }
+  statusCode;
+  responseData;
+  requestId;
+  static fromAxiosError(error) {
+    const status = error.response?.status;
+    const data = error.response?.data;
+    const requestId = extractRequestIdFromHeaders(error.response?.headers) ?? extractRequestIdFromBody(data);
+    const errorMessage = extractErrorMessage(data, error.message || "Unknown API error");
+    return new _APIError(
+      formatAPIErrorMessage(status, errorMessage, requestId),
+      status,
+      data,
+      requestId
+    );
+  }
+  static fromResponse(status, data, headers) {
+    const requestId = extractRequestIdFromHeaders(headers) ?? extractRequestIdFromBody(data);
+    const errorMessage = extractErrorMessage(data, `Request failed with status ${status}`);
+    return new _APIError(
+      formatAPIErrorMessage(status, errorMessage, requestId),
+      status,
+      data,
+      requestId
+    );
+  }
+  static unauthorizedApiKey(error) {
+    const requestId = extractRequestIdFromHeaders(error.response?.headers) ?? extractRequestIdFromBody(error.response?.data);
+    const message = "Authentication failed. Please update your API key.";
+    return new _APIError(formatAPIErrorMessage(401, message, requestId), 401, void 0, requestId);
+  }
+  static unauthorizedToken(error) {
+    const requestId = extractRequestIdFromHeaders(error.response?.headers) ?? extractRequestIdFromBody(error.response?.data);
+    const message = "Authentication expired. Please log in again.";
+    return new _APIError(formatAPIErrorMessage(401, message, requestId), 401, void 0, requestId);
+  }
+  static paymentRequired(error) {
+    const requestId = extractRequestIdFromHeaders(error.response?.headers) ?? extractRequestIdFromBody(error.response?.data);
+    const message = "Payment is required. Please complete checkout and try again.";
+    return new _APIError(formatAPIErrorMessage(402, message, requestId), 402, void 0, requestId);
+  }
+};
+function isRetriableError(error) {
+  if (typeof error === "object" && error !== null && error.isAxiosError) {
+    return isRetriableAxiosError(error);
+  }
+  if (error instanceof APIError) {
+    return typeof error.statusCode === "number" ? isRetriableHTTPStatus(error.statusCode) : false;
+  }
+  if (!(error instanceof Error)) {
+    return false;
+  }
+  const message = error.message;
+  const statusMatch = /(?:API Error \(|Failed to download [^:]+: )(\d{3})\b/i.exec(message);
+  if (statusMatch) {
+    return isRetriableHTTPStatus(Number.parseInt(statusMatch[1], 10));
+  }
+  const lower = message.toLowerCase();
+  return lower.includes("network") || lower.includes("timeout") || lower.includes("fetch failed") || lower.includes("econnaborted") || lower.includes("etimedout") || lower.includes("enotfound") || lower.includes("econnrefused") || lower.includes("err_network");
+}
+
+// src/http-client.ts
+var HttpClient = class {
+  constructor(options, runtime) {
+    this.runtime = runtime;
+    this.root = (options.root ?? DEFAULT_ROOT).replace(/\/$/, "");
+    this.token = options.token;
+    this.apiKey = options.apiKey;
+    this.spaceId = options.spaceId;
+    this.spaceIdExplicit = Boolean(options.spaceId);
+    this.onUnauthorized = options.onUnauthorized;
+    this.onPaymentRequired = options.onPaymentRequired;
+    this.allowGuestFallback = options.allowGuestFallback ?? true;
+    this.retryMutations = options.retryMutations ?? true;
+    this.authUuidPromise = runtime.resolveAuthUuid(options);
+    this.client = axios2.create({
+      baseURL: this.root,
+      headers: this.buildAuthHeaders(),
+      timeout: 3e4
+    });
+    this.client.interceptors.request.use(async (config) => {
+      const authUuid = await withSignal(this.authUuidPromise, config.signal);
+      throwIfAborted(config.signal);
+      const authHeaders = this.buildAuthHeaders();
+      const isFormData = typeof FormData !== "undefined" && config.data instanceof FormData;
+      if (isFormData) {
+        delete authHeaders["Content-Type"];
+      }
+      const headers = config.headers;
+      if (headers && typeof headers.set === "function") {
+        const mutable = headers;
+        mutable.delete?.("X-Auth-Token");
+        mutable.delete?.("Authorization");
+        if (isFormData) {
+          mutable.delete?.("Content-Type");
+        }
+        for (const [key, value] of Object.entries(authHeaders)) {
+          mutable.set(key, value);
+        }
+        mutable.set("X-Auth-UUID", authUuid);
+        return config;
+      }
+      const plain = { ...headers };
+      delete plain["X-Auth-Token"];
+      delete plain.Authorization;
+      if (isFormData) {
+        delete plain["Content-Type"];
+      }
+      config.headers = AxiosHeaders.from({
+        ...plain,
+        ...authHeaders,
+        "X-Auth-UUID": authUuid
+      });
+      return config;
+    });
+    this.client.interceptors.response.use(
+      (res) => res,
+      async (error) => {
+        if (!axios2.isAxiosError(error)) {
+          throw error;
+        }
+        const status = error.response?.status;
+        const cfg = error.config;
+        throwIfAborted(cfg?.signal);
+        if (status === 402 && cfg && !cfg.__deckopsCheckoutRetried) {
+          if (options.onPaymentRequired) {
+            cfg.__deckopsCheckoutRetried = true;
+            await withSignal(options.onPaymentRequired(), cfg.signal);
+            return await this.client.request(cfg);
+          }
+          throw APIError.paymentRequired(error);
+        }
+        if (status === 401 && cfg && !cfg.__deckopsAuthRetried) {
+          cfg.__deckopsAuthRetried = true;
+          const oldSpaceId = this.spaceIdFromConfig(cfg) ?? this.spaceId;
+          if (this.onUnauthorized && this.token && !this.isApiKeyAuth()) {
+            let auth;
+            try {
+              auth = await withSignal(this.refreshAuth(), cfg.signal);
+            } catch {
+              throwIfAborted(cfg.signal);
+              if (!this.allowGuestFallback) throw error;
+            }
+            if (auth && (typeof auth === "string" ? auth : auth.token)) {
+              this.applyAuthResult(auth, cfg, oldSpaceId);
+              return await this.client.request(cfg);
+            }
+          }
+          if (this.allowGuestFallback && (this.hasCredentials() || this.authDowngradedToGuest || this.guestDowngradePromise)) {
+            const guestSpaceId = await withSignal(this.ensureGuestMode(), cfg.signal);
+            this.rewriteRequestSpaceId(cfg, oldSpaceId, guestSpaceId);
+            cfg.headers = this.guestRequestHeaders(cfg.headers);
+            return await this.client.request(cfg);
+          }
+        }
+        throw error;
+      }
+    );
+  }
+  runtime;
+  client;
+  authUuidPromise;
+  root;
+  token;
+  apiKey;
+  spaceId;
+  spaceIdExplicit = false;
+  resolvedSpaceIdPromise;
+  authRefreshPromise;
+  guestDowngradePromise;
+  /** Set after credentials are cleared due to 401; later parallel 401s still retry as guest. */
+  authDowngradedToGuest = false;
+  onUnauthorized;
+  onPaymentRequired;
+  allowGuestFallback;
+  retryMutations;
+  async withRetry(request, signal, retry = true) {
+    const delays = getRetryDelaysMs();
+    let lastError;
+    for (let attempt = 0; attempt <= delays.length; attempt++) {
+      throwIfAborted(signal);
+      if (attempt > 0) {
+        await delay(delays[attempt - 1] ?? delays[delays.length - 1], signal);
+      }
+      try {
+        return await request();
+      } catch (error) {
+        throwIfAborted(signal);
+        lastError = error;
+        if (!retry || axios2.isCancel(error) || !axios2.isAxiosError(error) || !isRetriableAxiosError(error) || attempt >= delays.length) {
+          throw error;
+        }
+      }
+    }
+    throw lastError;
+  }
+  setToken(token) {
+    this.token = token;
+    if (token) {
+      this.authDowngradedToGuest = false;
+    }
+    this.clearAutoResolvedSpaceId();
+    this.applyAuthHeaders();
+  }
+  setApiKey(apiKey) {
+    this.apiKey = apiKey;
+    if (apiKey) {
+      this.authDowngradedToGuest = false;
+    }
+    this.clearAutoResolvedSpaceId();
+    this.applyAuthHeaders();
+  }
+  setSpaceId(spaceId) {
+    this.spaceId = spaceId;
+    this.spaceIdExplicit = Boolean(spaceId);
+    this.resolvedSpaceIdPromise = void 0;
+  }
+  async resolveSpaceId(spaceId, signal) {
+    throwIfAborted(signal);
+    if (spaceId) {
+      return spaceId;
+    }
+    if (this.spaceId) {
+      return this.spaceId;
+    }
+    if (signal) return await this.fetchDefaultSpaceId(signal);
+    if (!this.resolvedSpaceIdPromise) {
+      this.resolvedSpaceIdPromise = this.fetchDefaultSpaceId();
+    }
+    try {
+      return await this.resolvedSpaceIdPromise;
+    } catch (error) {
+      this.resolvedSpaceIdPromise = void 0;
+      throw error;
+    }
+  }
+  async fetchDefaultSpaceId(signal) {
+    const res = await this.get("/user", { signal });
+    const id = res.data.id;
+    if (!id) {
+      throw new Error("user.self did not return an id");
+    }
+    this.spaceId = id;
+    return id;
+  }
+  clearAutoResolvedSpaceId() {
+    if (this.spaceIdExplicit) {
+      return;
+    }
+    this.spaceId = void 0;
+    this.resolvedSpaceIdPromise = void 0;
+  }
+  getAuthUuid() {
+    return this.authUuidPromise;
+  }
+  url(path) {
+    return `${this.root}/${path.replace(/^\//, "")}`;
+  }
+  async get(path, config) {
+    try {
+      const request = () => this.client.get(this.url(path), config);
+      const res = config?.responseType === "stream" ? await request() : await this.withRetry(request, config?.signal);
+      return { data: res.data, headers: res.headers };
+    } catch (error) {
+      throwIfAborted(config?.signal);
+      if (axios2.isCancel(error)) throw error;
+      if (axios2.isAxiosError(error)) {
+        throw APIError.fromAxiosError(error);
+      }
+      throw error;
+    }
+  }
+  async post(path, data, config) {
+    try {
+      const res = await this.withRetry(() => this.client.post(this.url(path), data, config), config?.signal, this.retryMutations);
+      return { data: res.data, headers: res.headers };
+    } catch (error) {
+      throwIfAborted(config?.signal);
+      if (axios2.isCancel(error)) throw error;
+      if (axios2.isAxiosError(error)) {
+        throw APIError.fromAxiosError(error);
+      }
+      throw error;
+    }
+  }
+  async put(path, data, config) {
+    try {
+      const res = await this.withRetry(() => this.client.put(this.url(path), data, config), config?.signal);
+      return { data: res.data, headers: res.headers };
+    } catch (error) {
+      throwIfAborted(config?.signal);
+      if (axios2.isCancel(error)) throw error;
+      if (axios2.isAxiosError(error)) {
+        throw APIError.fromAxiosError(error);
+      }
+      throw error;
+    }
+  }
+  async delete(path, config) {
+    try {
+      await this.withRetry(() => this.client.delete(this.url(path), config), config?.signal);
+    } catch (error) {
+      throwIfAborted(config?.signal);
+      if (axios2.isCancel(error)) throw error;
+      if (axios2.isAxiosError(error)) {
+        throw APIError.fromAxiosError(error);
+      }
+      throw error;
+    }
+  }
+  async eventStream(path, config = {}) {
+    if (this.shouldUseFetchStream()) {
+      return await this.fetchEventStream(path, config);
+    }
+    return await this.get(path, {
+      headers: config.headers,
+      responseType: "stream",
+      signal: config.signal,
+      params: config.params
+    });
+  }
+  buildAuthHeaders() {
+    const headers = {
+      "Content-Type": "application/json"
+    };
+    if (this.token) {
+      headers["X-Auth-Token"] = this.token;
+    }
+    if (this.apiKey) {
+      headers.Authorization = `Bearer ${this.apiKey}`;
+    }
+    return headers;
+  }
+  shouldUseFetchStream() {
+    if (this.runtime.useFetchStreams) return true;
+    return typeof fetch === "function" && typeof ReadableStream !== "undefined" && !(typeof process !== "undefined" && process.versions?.node);
+  }
+  async fetchEventStream(path, config) {
+    let checkoutRetried = false;
+    let authRetried = false;
+    const params = { ...config.params ?? {} };
+    for (let networkRetry = 0; ; networkRetry++) {
+      throwIfAborted(config.signal);
+      const retryDelays = getRetryDelaysMs();
+      if (networkRetry > 0) {
+        await delay(retryDelays[networkRetry - 1] ?? retryDelays[retryDelays.length - 1], config.signal);
+      }
+      let response;
+      try {
+        const headers = {
+          ...this.buildAuthHeaders(),
+          "X-Auth-UUID": await withSignal(this.authUuidPromise, config.signal),
+          ...config.headers ?? {}
+        };
+        response = await fetch(this.urlWithParams(path, params), {
+          method: "GET",
+          headers,
+          signal: config.signal
+        });
+      } catch (error) {
+        throwIfAborted(config.signal);
+        if (networkRetry < getRetryDelaysMs().length && this.isRetriableFetchError(error)) {
+          continue;
+        }
+        throw error;
+      }
+      const responseHeaders = this.headersFromFetch(response.headers);
+      if (response.status === 402 && !checkoutRetried) {
+        if (this.onPaymentRequired) {
+          checkoutRetried = true;
+          await withSignal(this.onPaymentRequired(), config.signal);
+          networkRetry = 0;
+          continue;
+        }
+        throw await this.readFetchAPIError(response, responseHeaders);
+      }
+      if (response.status === 401 && !authRetried) {
+        authRetried = true;
+        const oldSpaceId = params.spaceId ?? this.spaceId;
+        if (this.onUnauthorized && this.token && !this.isApiKeyAuth()) {
+          try {
+            const auth = await withSignal(this.refreshAuth(), config.signal);
+            if (!(typeof auth === "string" ? auth : auth.token)) throw new Error("Token refresh returned no token");
+            const nextSpaceId = this.applyAuthResult(auth, void 0, oldSpaceId);
+            if (oldSpaceId && nextSpaceId && params.spaceId === oldSpaceId) {
+              params.spaceId = nextSpaceId;
+            }
+            networkRetry = 0;
+            continue;
+          } catch {
+            throwIfAborted(config.signal);
+            if (!this.allowGuestFallback) throw await this.readFetchAPIError(response, responseHeaders);
+          }
+        }
+        if (this.allowGuestFallback && (this.hasCredentials() || this.authDowngradedToGuest || this.guestDowngradePromise)) {
+          const guestSpaceId = await withSignal(this.ensureGuestMode(), config.signal);
+          if (oldSpaceId && params.spaceId === oldSpaceId) {
+            params.spaceId = guestSpaceId;
+          }
+          networkRetry = 0;
+          continue;
+        }
+      }
+      if (!response.ok) {
+        if (networkRetry < getRetryDelaysMs().length && (response.status === 604 || response.status === 502)) {
+          continue;
+        }
+        throw await this.readFetchAPIError(response, responseHeaders);
+      }
+      const contentType = response.headers.get("content-type")?.toLowerCase() ?? "";
+      if (contentType.includes("application/json")) {
+        return { data: await response.json(), headers: responseHeaders };
+      }
+      if (!response.body) {
+        throw new Error("Response body is not available for event stream");
+      }
+      return { data: response.body, headers: responseHeaders };
+    }
+  }
+  isApiKeyAuth() {
+    return Boolean(this.apiKey && !this.token);
+  }
+  hasCredentials() {
+    return Boolean(this.token || this.apiKey);
+  }
+  /**
+   * Clear expired credentials and resolve a guest space via GET /user
+   * (X-Auth-UUID only). Concurrent 401s share one downgrade.
+   */
+  ensureGuestMode() {
+    if (!this.hasCredentials() && this.spaceId && !this.guestDowngradePromise) {
+      return Promise.resolve(this.spaceId);
+    }
+    return this.downgradeToGuest();
+  }
+  downgradeToGuest() {
+    if (!this.guestDowngradePromise) {
+      this.authDowngradedToGuest = true;
+      this.guestDowngradePromise = (async () => {
+        this.token = void 0;
+        this.apiKey = void 0;
+        this.applyAuthHeaders();
+        this.spaceId = void 0;
+        this.spaceIdExplicit = false;
+        this.resolvedSpaceIdPromise = void 0;
+        const guestSpaceId = await this.resolveSpaceId();
+        if (!guestSpaceId) {
+          throw new Error("Failed to resolve guest space id after auth downgrade");
+        }
+        return guestSpaceId;
+      })().finally(() => {
+        this.guestDowngradePromise = void 0;
+      });
+    }
+    return this.guestDowngradePromise;
+  }
+  guestRequestHeaders(headers) {
+    const next = { ...headers ?? {} };
+    delete next["X-Auth-Token"];
+    delete next.Authorization;
+    return { ...next, ...this.buildAuthHeaders() };
+  }
+  spaceIdFromConfig(cfg) {
+    if (cfg.params && typeof cfg.params.spaceId === "string") {
+      return cfg.params.spaceId;
+    }
+    if (typeof FormData !== "undefined" && cfg.data instanceof FormData) {
+      const spaceId = cfg.data.get("spaceId");
+      return typeof spaceId === "string" ? spaceId : void 0;
+    }
+    if (typeof cfg.data === "string") {
+      try {
+        const parsed = JSON.parse(cfg.data);
+        if (typeof parsed.spaceId === "string") {
+          return parsed.spaceId;
+        }
+      } catch {
+      }
+    } else if (cfg.data && typeof cfg.data === "object") {
+      const spaceId = cfg.data.spaceId;
+      if (typeof spaceId === "string") {
+        return spaceId;
+      }
+    }
+    if (typeof cfg.url === "string") {
+      const match = cfg.url.match(/\/spaces\/([^/?#]+)\//);
+      if (match?.[1]) {
+        return decodeURIComponent(match[1]);
+      }
+    }
+    return void 0;
+  }
+  isRetriableFetchError(error) {
+    if (error instanceof DOMException && error.name === "AbortError") {
+      return false;
+    }
+    if (!(error instanceof Error)) {
+      return true;
+    }
+    const message = error.message.toLowerCase();
+    return message.includes("network") || message.includes("timeout") || message.includes("fetch failed");
+  }
+  async readFetchAPIError(response, headers, fallbackMessage) {
+    const text = await response.text();
+    let data = text;
+    if (text) {
+      try {
+        data = JSON.parse(text);
+      } catch {
+      }
+    }
+    const apiError = APIError.fromResponse(response.status, data, headers);
+    if (fallbackMessage) {
+      const requestIdSuffix = apiError.requestId ? ` [X-RequestId: ${apiError.requestId}]` : "";
+      return new APIError(
+        `API Error (${response.status}): ${fallbackMessage}${requestIdSuffix}`,
+        response.status,
+        apiError.responseData,
+        apiError.requestId
+      );
+    }
+    return apiError;
+  }
+  urlWithParams(path, params) {
+    const url = new URL(this.url(path));
+    for (const [key, value] of Object.entries(params ?? {})) {
+      url.searchParams.set(key, value);
+    }
+    return url.toString();
+  }
+  headersFromFetch(headers) {
+    const result = {};
+    headers.forEach((value, key) => {
+      result[key] = value;
+    });
+    return result;
+  }
+  refreshAuth() {
+    if (!this.onUnauthorized) {
+      return Promise.reject(new Error("onUnauthorized is not configured"));
+    }
+    if (!this.authRefreshPromise) {
+      this.authRefreshPromise = this.onUnauthorized().then((auth) => {
+        const token = typeof auth === "string" ? auth : auth?.token;
+        if (typeof token !== "string" || !token.trim()) {
+          throw new Error("onUnauthorized must return a non-empty token");
+        }
+        return auth;
+      }).finally(() => {
+        this.authRefreshPromise = void 0;
+      });
+    }
+    return this.authRefreshPromise;
+  }
+  applyAuthResult(auth, cfg, oldSpaceId) {
+    const nextToken = typeof auth === "string" ? auth : auth.token;
+    const nextSpaceId = typeof auth === "string" ? this.spaceId : auth.spaceId;
+    const previousSpaceId = oldSpaceId ?? this.spaceId;
+    this.setToken(nextToken);
+    if (nextSpaceId) {
+      this.setSpaceId(nextSpaceId);
+    }
+    if (cfg) {
+      this.rewriteRequestSpaceId(cfg, previousSpaceId, nextSpaceId);
+      cfg.headers = { ...cfg.headers ?? {}, ...this.buildAuthHeaders() };
+    }
+    return nextSpaceId;
+  }
+  applyAuthHeaders() {
+    const headers = this.buildAuthHeaders();
+    const common = this.client.defaults.headers.common;
+    if (typeof common.delete === "function") {
+      common.delete("X-Auth-Token");
+      common.delete("Authorization");
+    } else {
+      delete common["X-Auth-Token"];
+      delete common.Authorization;
+    }
+    for (const [key, value] of Object.entries(headers)) {
+      common[key] = value;
+    }
+  }
+  rewriteRequestSpaceId(cfg, oldSpaceId, newSpaceId) {
+    if (!oldSpaceId || !newSpaceId || oldSpaceId === newSpaceId) {
+      return;
+    }
+    const encodedOld = encodeURIComponent(oldSpaceId);
+    const encodedNew = encodeURIComponent(newSpaceId);
+    if (typeof cfg.url === "string") {
+      cfg.url = cfg.url.replace(`/spaces/${encodedOld}/`, `/spaces/${encodedNew}/`);
+    }
+    if (cfg.params && cfg.params.spaceId === oldSpaceId) {
+      cfg.params.spaceId = newSpaceId;
+    }
+    if (!cfg.data) {
+      return;
+    }
+    if (typeof FormData !== "undefined" && cfg.data instanceof FormData) {
+      if (cfg.data.get("spaceId") === oldSpaceId) cfg.data.set("spaceId", newSpaceId);
+      return;
+    }
+    if (typeof cfg.data === "string") {
+      try {
+        const parsed = JSON.parse(cfg.data);
+        if (parsed.spaceId === oldSpaceId) {
+          parsed.spaceId = newSpaceId;
+          cfg.data = JSON.stringify(parsed);
+        }
+      } catch {
+      }
+    } else if (typeof cfg.data === "object" && cfg.data.spaceId === oldSpaceId) {
+      cfg.data.spaceId = newSpaceId;
+    }
+  }
+};
+
+// src/parse/types.ts
+var IR_RETENTION_DAYS = 7;
+var PAGE_SEPARATOR = "\n\n---\n\n";
+
+// src/parse/index.ts
+var PARSE_TASK_TYPE_BY_EXTENSION = {
+  ".pdf": "pdf.pdfParse",
+  ".pptx": "pptx.parse",
+  ".docx": "docx.parseTextAndImage",
+  ".key": "keynote.parseTextAndImage"
+};
+var PARSE_SUPPORTED_EXTENSIONS = Object.keys(PARSE_TASK_TYPE_BY_EXTENSION);
+var PARSE_PAGED_TASK_TYPES = /* @__PURE__ */ new Set([
+  "pptx.parse",
+  "keynote.parseTextAndImage"
+]);
+var extensionOf = (nameOrPath) => {
+  const clean = nameOrPath.split(/[?#]/)[0] ?? "";
+  const base = clean.split(/[\\/]/).pop() ?? "";
+  const dot = base.lastIndexOf(".");
+  return dot > 0 ? base.slice(dot).toLowerCase() : "";
+};
+var parseTaskTypeFor = (nameOrPath) => PARSE_TASK_TYPE_BY_EXTENSION[extensionOf(nameOrPath)];
+
+// src/parse-facade.ts
+var isLinkSource = (source) => typeof source === "object" && source !== null && "url" in source;
+var isFileIdSource = (source) => typeof source === "object" && source !== null && "fileId" in source;
+var nameForRouting = (source) => {
+  if (source.name) return source.name;
+  if (typeof source.file === "string") return source.file;
+  if (typeof source.file === "object" && source.file !== null && "input" in source.file) {
+    const nested = source.file;
+    if (nested.name) return nested.name;
+    if (typeof nested.input === "string") return nested.input;
+  }
+  const maybeNamed = source.file;
+  return maybeNamed?.name ?? "";
+};
+var unsupported = (name) => new Error(
+  `Cannot determine parser for ${name ? `"${name}"` : "input"}. Supported extensions: ${PARSE_SUPPORTED_EXTENSIONS.join(", ")}. Pass { name } to specify the file name.`
+);
+var parseParams = (type, options) => {
+  const params = {};
+  switch (type) {
+    case "pdf.pdfParse":
+      if (options.password !== void 0) params.password = options.password;
+      if (options.parseProfile !== void 0) params.parseProfile = options.parseProfile;
+      if (options.includeImages !== void 0) params.includeImages = options.includeImages;
+      break;
+    case "keynote.parseTextAndImage":
+      if (options.stayImageAreaRate !== void 0) params.stayImageAreaRate = options.stayImageAreaRate;
+      break;
+    case "pptx.parse":
+    case "docx.parseTextAndImage":
+    case "html.getByURL":
+      break;
+  }
+  return params;
+};
+var toParseResult = (raw, taskId, type) => {
+  const body = typeof raw === "object" && raw !== null ? raw : {};
+  if (typeof body.irKey !== "string" || !body.irKey) {
+    throw new Error(
+      `${type} returned no irKey. The backend is likely older than the parse/convert split (@deckflow/platform-slave 0.22.0); its result cannot be converted without re-parsing.`
+    );
+  }
+  return {
+    taskId,
+    type,
+    irKey: body.irKey,
+    irSchemaVersion: body.irSchemaVersion ?? "",
+    ir: raw
+  };
+};
+var createParse = (deps) => {
+  const parse = async (source, options = {}) => {
+    const signal = options.signal ?? options.wait?.signal;
+    throwIfAborted(signal);
+    if (isLinkSource(source)) {
+      const task2 = await deps.createTask({
+        signal,
+        type: "html.getByURL",
+        spaceId: options.spaceId,
+        params: {
+          url: source.url,
+          mode: source.mode ?? "runtime",
+          ...parseParams("html.getByURL", options)
+        }
+      });
+      options.onTask?.(task2);
+      throwIfAborted(signal);
+      const spaceId2 = task2.spaceId ?? options.spaceId;
+      const done2 = await deps.waitTask(task2.id, { ...options.wait, signal, spaceId: spaceId2 });
+      const raw2 = await deps.downTask(done2.id, { signal, spaceId: spaceId2 });
+      return toParseResult(raw2, done2.id, "html.getByURL");
+    }
+    const normalized = typeof source === "string" ? { file: source } : source;
+    const name = isFileIdSource(normalized) ? normalized.name : nameForRouting(normalized);
+    const type = parseTaskTypeFor(name);
+    if (!type) throw unsupported(name);
+    const task = await deps.createTask({
+      signal,
+      upload: {
+        ...options.upload,
+        ...isFileIdSource(normalized) || !normalized.name ? {} : { name: normalized.name },
+        signal
+      },
+      type,
+      spaceId: options.spaceId,
+      params: parseParams(type, options),
+      ...isFileIdSource(normalized) ? { fileIds: [normalized.fileId] } : { files: [normalized.file] }
+    });
+    options.onTask?.(task);
+    throwIfAborted(signal);
+    const spaceId = task.spaceId ?? options.spaceId;
+    const done = await deps.waitTask(task.id, { ...options.wait, signal, spaceId });
+    const raw = await deps.downTask(done.id, { signal, spaceId });
+    return toParseResult(raw, done.id, type);
+  };
+  const convert = async (ref, options = {}) => {
+    const signal = options.signal ?? options.wait?.signal;
+    throwIfAborted(signal);
+    const task = await deps.createTask({
+      signal,
+      type: "parse.convert",
+      spaceId: options.spaceId,
+      params: {
+        ...ref.irKey ? { irKey: ref.irKey } : { taskId: ref.taskId },
+        to: options.to ?? "markdown",
+        ...options.markdownMeta === void 0 ? {} : { markdownMeta: options.markdownMeta },
+        ...options.markdownPages === void 0 ? {} : { markdownPages: options.markdownPages },
+        ...options.markdownStrict === void 0 ? {} : { markdownStrict: options.markdownStrict }
+      }
+    });
+    options.onTask?.(task);
+    throwIfAborted(signal);
+    const spaceId = task.spaceId ?? options.spaceId;
+    const done = await deps.waitTask(task.id, { ...options.wait, signal, spaceId });
+    const raw = await deps.downTask(done.id, { signal, spaceId });
+    return { ...raw, taskId: done.id };
+  };
+  return { parse, convert };
+};
+
+// src/tasks.ts
+import { createParser } from "eventsource-parser";
+var SSE_RETRY_INTERVAL = 5e3;
+var SSE_MAX_RETRIES = 100;
+var EventStreamUnavailableError = class extends Error {
+};
+var TasksApi = class {
+  constructor(http, files) {
+    this.http = http;
+    this.files = files;
+  }
+  http;
+  files;
+  webStreamReaders = /* @__PURE__ */ new WeakMap();
+  async create(params) {
+    throwIfAborted(params.signal);
+    const spaceId = await this.http.resolveSpaceId(params.spaceId, params.signal);
+    const prepared = await this.prepareTaskFiles(params);
+    const totalBytes = prepared.reduce((sum, file) => sum + file.bytes, 0);
+    const canInlineFiles = prepared.length > 0 && !params.fileIds?.length && totalBytes < INLINE_TASK_FILES_MAX_BYTES;
+    if (canInlineFiles) {
+      return await this.createWithInlineFiles(spaceId, params, prepared);
+    }
+    const fileIds = await this.resolveFileIds(spaceId, params, prepared);
+    const payload = {
+      fileIds,
+      type: params.type,
+      params: params.params ?? {}
+    };
+    if (spaceId) {
+      payload.spaceId = spaceId;
+    }
+    if (params.name) {
+      payload.name = params.name;
+    }
+    const res = await this.http.post("/tools/tasks", payload, { signal: params.signal });
+    return res.data;
+  }
+  async createWithInlineFiles(spaceId, params, files) {
+    if (typeof globalThis.FormData === "undefined" || typeof globalThis.Blob === "undefined") {
+      throw new Error("FormData and Blob are required for inline task file uploads in this runtime");
+    }
+    const form = new FormData();
+    if (spaceId) {
+      form.append("spaceId", spaceId);
+    }
+    form.append("type", params.type);
+    form.append("params", JSON.stringify(params.params ?? {}));
+    if (params.name) {
+      form.append("name", params.name);
+    }
+    for (const file of files) {
+      const blob = typeof Blob !== "undefined" && file.data instanceof Blob ? file.data : new Blob([this.toArrayBuffer(file.data)]);
+      form.append("files", blob, file.name);
+    }
+    const res = await this.http.post("/tools/tasks", form, { signal: params.signal });
+    params.upload?.onProgress?.(1);
+    return res.data;
+  }
+  toArrayBuffer(data) {
+    const copy = new ArrayBuffer(data.byteLength);
+    new Uint8Array(copy).set(data);
+    return copy;
+  }
+  /**
+   * Trigger an already-created task to begin executing.
+   *
+   * Required for guest-mode tasks: the backend creates them in a pending state
+   * and waits for an explicit `PUT /tools/tasks/:id/start` before running.
+   * Authenticated tasks are started automatically by the backend.
+   */
+  async start(taskId, options = {}) {
+    const res = await this.http.put(
+      `/tools/tasks/${encodeURIComponent(taskId)}/start`,
+      void 0,
+      { params: this.taskQueryParams(options.spaceId), signal: options.signal }
+    );
+    return res.data;
+  }
+  async prepareTaskFiles(params) {
+    if (!params.files?.length) {
+      return [];
+    }
+    if (!this.files) {
+      throw new Error("File upload is not available for this task client");
+    }
+    return await Promise.all(
+      params.files.map(async (file) => {
+        const { input, options } = this.normalizeTaskUpload(file, params.upload);
+        return await this.files.prepare(input, { ...options, signal: params.signal ?? options.signal });
+      })
+    );
+  }
+  async resolveFileIds(spaceId, params, prepared) {
+    const fileIds = [...params.fileIds ?? []];
+    if (!prepared.length) {
+      return fileIds;
+    }
+    if (!this.files) {
+      throw new Error("File upload is not available for this task client");
+    }
+    const uploaded = await Promise.all(
+      prepared.map(async (file, index) => {
+        const source = params.files[index];
+        const { options } = this.normalizeTaskUpload(source, params.upload);
+        const result = await this.files.uploadPrepared(file, {
+          ...options,
+          spaceId,
+          signal: params.signal ?? options.signal
+        });
+        return result.id;
+      })
+    );
+    return [...fileIds, ...uploaded];
+  }
+  normalizeTaskUpload(file, defaults = {}) {
+    if (this.isTaskUploadObject(file)) {
+      const { input, ...options } = file;
+      return {
+        input,
+        options: {
+          ...defaults,
+          ...options
+        }
+      };
+    }
+    return { input: file, options: defaults };
+  }
+  isTaskUploadObject(file) {
+    return typeof file === "object" && file !== null && "input" in file && !this.isBlob(file);
+  }
+  isBlob(value) {
+    return typeof Blob !== "undefined" && value instanceof Blob;
+  }
+  async list(params = {}) {
+    const spaceId = await this.http.resolveSpaceId(params.spaceId, params.signal);
+    const query = {
+      _startIndex: params.startIndex ?? 0,
+      _maxResults: params.maxResults ?? 50
+    };
+    if (spaceId) {
+      query.spaceId = spaceId;
+    }
+    if (params.type) {
+      query.type = params.type;
+    }
+    const res = await this.http.get("/tools/tasks", { params: query, signal: params.signal });
+    const total = res.headers["x-content-record-total"];
+    return {
+      tasks: res.data,
+      total: typeof total === "string" ? Number.parseInt(total, 10) : res.data.length
+    };
+  }
+  async get(taskId, options = {}) {
+    const headers = {};
+    if (options.useEventStream) {
+      headers["response-event-stream"] = "yes";
+    }
+    const res = await this.http.get(`/tools/tasks/${encodeURIComponent(taskId)}`, {
+      headers,
+      signal: options.signal,
+      params: this.taskQueryParams(options.spaceId)
+    });
+    const contentType = String(res.headers["content-type"] ?? "").toLowerCase();
+    if (contentType.includes("event-stream") || contentType.includes("text/event-stream")) {
+      const text = typeof res.data === "string" ? res.data : JSON.stringify(res.data);
+      for (const line of text.split("\n")) {
+        if (line.startsWith("data: ")) {
+          try {
+            return JSON.parse(line.slice(6));
+          } catch {
+          }
+        }
+      }
+    }
+    return res.data;
+  }
+  async delete(taskId, options = {}) {
+    await this.http.delete(`/tools/tasks/${encodeURIComponent(taskId)}`, {
+      params: this.taskQueryParams(options.spaceId),
+      signal: options.signal
+    });
+  }
+  async down(taskId, options = {}) {
+    const params = options.spaceId ? { spaceId: options.spaceId } : {};
+    if (options.type) {
+      params._type = options.type;
+    }
+    const res = await this.http.get(
+      `/tools/tasks/${encodeURIComponent(taskId)}/download`,
+      { ...Object.keys(params).length ? { params } : {}, signal: options.signal }
+    );
+    return res.data;
+  }
+  async wait(taskId, options = {}) {
+    const timeout = options.timeout ?? DEFAULT_TIMEOUT;
+    throwIfAborted(options.signal);
+    const controller = new AbortController();
+    const onAbort = () => controller.abort(options.signal?.reason);
+    options.signal?.addEventListener("abort", onAbort, { once: true });
+    const timer = setTimeout(() => controller.abort(new Error(`Task ${taskId} did not complete within ${timeout}s`)), timeout * 1e3);
+    try {
+      return await this.waitInternal(taskId, { ...options, signal: controller.signal });
+    } finally {
+      clearTimeout(timer);
+      options.signal?.removeEventListener("abort", onAbort);
+      controller.abort();
+    }
+  }
+  async waitInternal(taskId, options) {
+    const timeout = options.timeout ?? DEFAULT_TIMEOUT;
+    const startedAt = Date.now();
+    const current = await this.get(taskId, { signal: options.signal, spaceId: options.spaceId });
+    options.onProgress?.(current);
+    if (current.status === "completed") {
+      return current;
+    }
+    if (current.status === "failed") {
+      throw new Error(`Task failed: ${current.error || "Unknown error"}`);
+    }
+    const remainingTimeout = Math.max(timeout - (Date.now() - startedAt) / 1e3, 0);
+    if (options.useEventStream !== false) {
+      return await this.waitWithEventStream(
+        taskId,
+        remainingTimeout,
+        options.onProgress,
+        options.signal,
+        options.pollInterval ?? DEFAULT_POLL_INTERVAL,
+        options.spaceId
+      );
+    }
+    return await this.waitWithPolling(
+      taskId,
+      remainingTimeout,
+      options.pollInterval ?? DEFAULT_POLL_INTERVAL,
+      options.onProgress,
+      options.signal,
+      options.spaceId
+    );
+  }
+  subscribe(taskId, handlers) {
+    throwIfAborted(handlers.signal);
+    const abortController = new AbortController();
+    let retryTimer;
+    let resolveRetryTimer;
+    let activeStream;
+    let closed = false;
+    let retryCount = 0;
+    const cancel = () => {
+      closed = true;
+      handlers.signal?.removeEventListener("abort", cancel);
+      abortController.abort();
+      if (retryTimer) {
+        clearTimeout(retryTimer);
+        retryTimer = void 0;
+      }
+      resolveRetryTimer?.();
+      resolveRetryTimer = void 0;
+      this.destroyStream(activeStream);
+      activeStream = void 0;
+    };
+    handlers.signal?.addEventListener("abort", cancel, { once: true });
+    const waitBeforeRetry = async () => {
+      await new Promise((resolve) => {
+        resolveRetryTimer = resolve;
+        retryTimer = setTimeout(() => {
+          retryTimer = void 0;
+          resolveRetryTimer = void 0;
+          resolve();
+        }, SSE_RETRY_INTERVAL);
+      });
+    };
+    const run = async () => {
+      while (!closed) {
+        try {
+          await this.openTaskEventStream(taskId, handlers, abortController.signal, (stream) => {
+            activeStream = stream;
+          });
+          return;
+        } catch (error) {
+          if (closed || this.isCanceledError(error)) {
+            return;
+          }
+          if (!this.isSseTransportError(error) || retryCount >= SSE_MAX_RETRIES) {
+            handlers.onError?.(error);
+            return;
+          }
+          retryCount += 1;
+          await waitBeforeRetry();
+        }
+      }
+    };
+    void run().finally(() => handlers.signal?.removeEventListener("abort", cancel));
+    return Promise.resolve(cancel);
+  }
+  async openTaskEventStream(taskId, handlers, signal, onStream) {
+    throwIfAborted(signal);
+    const res = await this.http.eventStream(`/tools/tasks/${encodeURIComponent(taskId)}`, {
+      headers: { "response-event-stream": "yes" },
+      signal,
+      params: this.taskQueryParams(handlers.spaceId)
+    });
+    const contentType = String(res.headers["content-type"] ?? "").toLowerCase();
+    if (contentType.includes("application/json")) {
+      const task = await this.parseJsonTaskBody(res.data, signal);
+      handlers.onUpdate(task);
+      throwIfAborted(signal);
+      if (task.status !== "completed" && task.status !== "failed") {
+        throw new EventStreamUnavailableError("Task endpoint returned a non-terminal JSON snapshot instead of SSE");
+      }
+      return;
+    }
+    if (!contentType.includes("event-stream") && !contentType.includes("text/event-stream")) {
+      throw new Error(`Unexpected Content-Type: ${contentType}`);
+    }
+    await new Promise((resolve, reject) => {
+      let terminal = false;
+      const parser = createParser((event) => {
+        if (event.type !== "event") {
+          return;
+        }
+        try {
+          const task = JSON.parse(event.data);
+          handlers.onUpdate(task);
+          if (task.status === "completed" || task.status === "failed") {
+            terminal = true;
+            this.destroyStream(stream);
+            cleanup();
+            resolve();
+          }
+        } catch (error) {
+          handlers.onError?.(error);
+        }
+      });
+      const stream = res.data;
+      const cleanup = () => {
+        if (!this.isNodeReadable(stream)) {
+          onStream(void 0);
+          return;
+        }
+        stream.off("data", onData);
+        stream.off("error", onError);
+        stream.off("end", onEnd);
+        stream.off("close", onClose);
+        onStream(void 0);
+      };
+      const reconnect = (error) => {
+        cleanup();
+        if (terminal || signal.aborted) {
+          resolve();
+        } else {
+          reject(error);
+        }
+      };
+      const onData = (chunk) => {
+        parser.feed(this.streamChunkToText(chunk));
+      };
+      const onError = (error) => {
+        reconnect(error);
+      };
+      const onEnd = () => {
+        reconnect(new Error("SSE connection ended before task completion"));
+      };
+      const onClose = () => {
+        reconnect(new Error("SSE connection closed before task completion"));
+      };
+      onStream(stream);
+      if (typeof stream === "string") {
+        parser.feed(stream);
+        if (!terminal) {
+          reconnect(new Error("SSE connection ended before task completion"));
+        }
+        return;
+      }
+      if (this.isWebReadableStream(stream)) {
+        void this.consumeWebStream(stream, signal, (chunk) => parser.feed(chunk)).then(() => {
+          if (!terminal) {
+            reconnect(new Error("SSE connection ended before task completion"));
+          }
+        }).catch((error) => reconnect(error));
+        return;
+      }
+      stream.on("data", onData);
+      stream.on("error", onError);
+      stream.on("end", onEnd);
+      stream.on("close", onClose);
+    });
+  }
+  async parseJsonTaskBody(body, signal) {
+    if (typeof body === "object" && body !== null && !this.isNodeReadable(body) && !this.isWebReadableStream(body)) {
+      return body;
+    }
+    const text = (await this.readBodyAsText(body, signal)).trim();
+    if (!text) {
+      throw new Error("Empty JSON body in task detail response");
+    }
+    return JSON.parse(text);
+  }
+  async readBodyAsText(body, signal) {
+    if (typeof body === "string") {
+      return body;
+    }
+    if (this.isWebReadableStream(body)) {
+      const chunks = [];
+      await this.consumeWebStream(body, signal, (chunk) => {
+        chunks.push(chunk);
+      });
+      return chunks.join("");
+    }
+    if (this.isNodeReadable(body)) {
+      return await new Promise((resolve, reject) => {
+        const chunks = [];
+        const onData = (chunk) => {
+          if (chunk instanceof Uint8Array) {
+            chunks.push(chunk);
+            return;
+          }
+          chunks.push(new TextEncoder().encode(this.streamChunkToText(chunk)));
+        };
+        const onError = (error) => {
+          cleanup();
+          reject(error);
+        };
+        const onEnd = () => {
+          cleanup();
+          const total = chunks.reduce((sum, chunk) => sum + chunk.byteLength, 0);
+          const merged = new Uint8Array(total);
+          let offset = 0;
+          for (const chunk of chunks) {
+            merged.set(chunk, offset);
+            offset += chunk.byteLength;
+          }
+          resolve(new TextDecoder().decode(merged));
+        };
+        const onAbort = () => {
+          cleanup();
+          body.destroy?.();
+          reject(signal.reason ?? new Error("canceled"));
+        };
+        const cleanup = () => {
+          body.off("data", onData);
+          body.off("error", onError);
+          body.off("end", onEnd);
+          body.off("close", onEnd);
+          signal.removeEventListener("abort", onAbort);
+        };
+        if (signal.aborted) {
+          onAbort();
+          return;
+        }
+        signal.addEventListener("abort", onAbort, { once: true });
+        body.on("data", onData);
+        body.on("error", onError);
+        body.on("end", onEnd);
+        body.on("close", onEnd);
+      });
+    }
+    return String(body ?? "");
+  }
+  isSseTransportError(error) {
+    if (error instanceof EventStreamUnavailableError) return false;
+    if (typeof error !== "object" || error === null) {
+      return false;
+    }
+    const statusCode = error.statusCode;
+    if (typeof statusCode === "number") {
+      return false;
+    }
+    if (error instanceof Error && error.message.startsWith("Unexpected Content-Type:")) {
+      return false;
+    }
+    return true;
+  }
+  isCanceledError(error) {
+    if (typeof error !== "object" || error === null) {
+      return false;
+    }
+    const err = error;
+    return err.name === "AbortError" || err.code === "ERR_CANCELED" || err.message === "canceled" || err.message?.includes("canceled") === true;
+  }
+  destroyStream(stream) {
+    if (this.isWebReadableStream(stream)) {
+      const reader = this.webStreamReaders.get(stream);
+      if (reader) {
+        void reader.cancel().catch(() => {
+        });
+      } else {
+        void stream.cancel().catch(() => {
+        });
+      }
+      return;
+    }
+    const destroy = stream && this.isNodeReadable(stream) ? stream.destroy : void 0;
+    if (typeof destroy === "function") {
+      destroy.call(stream);
+    }
+  }
+  isNodeReadable(value) {
+    return typeof value === "object" && value !== null && typeof value.on === "function" && typeof value.off === "function";
+  }
+  isWebReadableStream(value) {
+    return typeof ReadableStream !== "undefined" && value instanceof ReadableStream && typeof value.getReader === "function";
+  }
+  streamChunkToText(chunk) {
+    if (typeof chunk === "string") {
+      return chunk;
+    }
+    if (chunk instanceof Uint8Array) {
+      return new TextDecoder().decode(chunk);
+    }
+    return String(chunk);
+  }
+  async consumeWebStream(stream, signal, onChunk) {
+    const reader = stream.getReader();
+    this.webStreamReaders.set(stream, reader);
+    const decoder = new TextDecoder();
+    const abort = () => {
+      void reader.cancel().catch(() => {
+      });
+    };
+    signal.addEventListener("abort", abort, { once: true });
+    try {
+      throwIfAborted(signal);
+      for (; ; ) {
+        const { done, value } = await reader.read();
+        throwIfAborted(signal);
+        if (done) {
+          break;
+        }
+        onChunk(decoder.decode(value, { stream: true }));
+      }
+      const rest = decoder.decode();
+      if (rest) {
+        onChunk(rest);
+      }
+    } finally {
+      this.webStreamReaders.delete(stream);
+      signal.removeEventListener("abort", abort);
+      reader.releaseLock();
+    }
+  }
+  async waitWithEventStream(taskId, timeout, onProgress, signal, pollInterval = DEFAULT_POLL_INTERVAL, spaceId) {
+    return await new Promise((resolve, reject) => {
+      throwIfAborted(signal);
+      const start = Date.now();
+      let cancel;
+      let settled = false;
+      let fallbackStarted = false;
+      const remainingTimeout = () => {
+        const elapsedSeconds = (Date.now() - start) / 1e3;
+        return Math.max(timeout - elapsedSeconds, 0);
+      };
+      const finish = (callback) => {
+        signal?.removeEventListener("abort", onAbort);
+        clearInterval(timer);
+        cancel?.();
+        if (!settled) {
+          settled = true;
+          callback();
+        }
+      };
+      const onAbort = () => finish(() => reject(signal?.reason ?? new DOMException("Aborted", "AbortError")));
+      signal?.addEventListener("abort", onAbort, { once: true });
+      const fallbackToPolling = () => {
+        if (settled || fallbackStarted) {
+          return;
+        }
+        fallbackStarted = true;
+        clearInterval(timer);
+        cancel?.();
+        this.waitWithPolling(taskId, remainingTimeout(), pollInterval, onProgress, signal, spaceId).then((task) => {
+          finish(() => resolve(task));
+        }).catch((error) => {
+          finish(() => reject(error));
+        });
+      };
+      const timer = setInterval(() => {
+        if (Date.now() - start > timeout * 1e3) {
+          finish(() => reject(new Error(`Task ${taskId} did not complete within ${timeout}s`)));
+        }
+      }, 1e3);
+      void this.subscribe(taskId, {
+        signal,
+        spaceId,
+        onUpdate: (task) => {
+          onProgress?.(task);
+          if (task.status === "completed") {
+            finish(() => resolve(task));
+          } else if (task.status === "failed") {
+            finish(() => reject(new Error(`Task failed: ${task.error || "Unknown error"}`)));
+          }
+        },
+        onError: (error) => {
+          if (signal?.aborted || error.statusCode === 401) {
+            finish(() => reject(signal?.aborted ? signal.reason : error));
+            return;
+          }
+          fallbackToPolling();
+        }
+      }).then((cancelFn) => {
+        cancel = cancelFn;
+        if (settled || fallbackStarted) {
+          cancelFn();
+        }
+      }).catch((error) => finish(() => reject(error)));
+    });
+  }
+  async waitWithPolling(taskId, timeout, pollInterval, onProgress, signal, spaceId) {
+    const start = Date.now();
+    for (; ; ) {
+      throwIfAborted(signal);
+      if (Date.now() - start > timeout * 1e3) {
+        throw new Error(`Task ${taskId} did not complete within ${timeout}s`);
+      }
+      const task = await this.get(taskId, { signal, spaceId });
+      onProgress?.(task);
+      if (task.status === "completed") {
+        return task;
+      }
+      if (task.status === "failed") {
+        throw new Error(`Task failed: ${task.error || "Unknown error"}`);
+      }
+      await delay(pollInterval, signal);
+    }
+  }
+  taskQueryParams(spaceId) {
+    const effectiveSpaceId = spaceId ?? this.http.spaceId;
+    return effectiveSpaceId ? { spaceId: effectiveSpaceId } : void 0;
+  }
+};
+
+// src/client.ts
+function createDeckClient(options, runtime) {
+  const http = new HttpClient(options, runtime);
+  const files = new FilesApi(http, runtime);
+  const tasks = new TasksApi(http, files);
+  const shortcut = (type) => {
+    return (params) => tasks.create({
+      ...params,
+      params: params.params ?? {},
+      type
+    });
+  };
+  const { parse, convert } = createParse({
+    createTask: (params) => tasks.create(params),
+    waitTask: (taskId, options2) => tasks.wait(taskId, options2),
+    downTask: (taskId, options2) => tasks.down(taskId, options2)
+  });
+  return {
+    root: http.root,
+    tasks,
+    ttask: tasks,
+    files,
+    setToken: (token) => http.setToken(token),
+    setApiKey: (apiKey) => http.setApiKey(apiKey),
+    setSpaceId: (spaceId) => http.setSpaceId(spaceId),
+    getAuthUuid: () => http.getAuthUuid(),
+    fileCompress: shortcut("file.compress"),
+    imageOcr: shortcut("image.ocr"),
+    imageConvertWebp: shortcut("image.convertWebp"),
+    imageResize: shortcut("image.resize"),
+    pptxSplit: shortcut("pptx.split"),
+    pptxJoin: shortcut("pptx.join"),
+    pptxGetFontInfo: shortcut("pptx.getFontInfo"),
+    pptxGetTextShapes: shortcut("pptx.getTextShapes"),
+    pptxEmbedFonts: shortcut("pptx.embedFonts"),
+    convertPptToImage: shortcut("convertor.ppt2image"),
+    convertPptToPptx: shortcut("convertor.ppt2pptx"),
+    convertPptToPdf: shortcut("convertor.ppt2pdf"),
+    convertDocToPdf: shortcut("convertor.doc2pdf"),
+    convertPptToVideo: shortcut("convertor.ppt2video"),
+    convertPdfToImage: shortcut("convertor.pdf2image"),
+    convertKeynoteToImage: shortcut("convertor.keynote2image"),
+    convertKeynoteToHtml: shortcut("convertor.keynote2html"),
+    convertKeynoteToPdf: shortcut("convertor.keynote2pdf"),
+    convertHtmlToPng: shortcut("convertor.html2png"),
+    convertMarkdownToPng: shortcut("convertor.markdown2png"),
+    convertHtmlToPptx: shortcut("convertor.html2pptx"),
+    htmlBuildPlayer: shortcut("html.buildPlayer"),
+    generation: shortcut("generation"),
+    translation: shortcut("translation"),
+    revamp: shortcut("revamp"),
+    pdfParse: shortcut("pdf.pdfParse"),
+    pptxParse: shortcut("pptx.parse"),
+    docxParse: shortcut("docx.parseTextAndImage"),
+    keynoteParse: shortcut("keynote.parseTextAndImage"),
+    htmlGetByURL: shortcut("html.getByURL"),
+    convertIr: shortcut("parse.convert"),
+    parse,
+    convert
+  };
+}
+
+// src/browser-auth-uuid.ts
+var UUID_V4_RE = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
+var defaultResolving;
+function isValidAuthUuid(value) {
+  return typeof value === "string" && UUID_V4_RE.test(value);
+}
+function generateAuthUuid() {
+  if (typeof globalThis.crypto?.randomUUID === "function") return globalThis.crypto.randomUUID();
+  const bytes = globalThis.crypto.getRandomValues(new Uint8Array(16));
+  bytes[6] = bytes[6] & 15 | 64;
+  bytes[8] = bytes[8] & 63 | 128;
+  const hex = [...bytes].map((byte) => byte.toString(16).padStart(2, "0")).join("");
+  return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;
+}
+async function resolveAuthUuid(options = {}) {
+  if (isValidAuthUuid(options.authUuid)) return options.authUuid;
+  if (options.authUuidStorage) {
+    const stored = await options.authUuidStorage.get();
+    if (isValidAuthUuid(stored)) return stored;
+    const generated = generateAuthUuid();
+    await options.authUuidStorage.set(generated);
+    return generated;
+  }
+  defaultResolving ??= Promise.resolve().then(() => {
+    try {
+      const stored = globalThis.localStorage?.getItem("df_uuid");
+      if (isValidAuthUuid(stored)) return stored;
+    } catch {
+    }
+    const generated = generateAuthUuid();
+    try {
+      globalThis.localStorage?.setItem("df_uuid", generated);
+    } catch {
+    }
+    return generated;
+  });
+  return defaultResolving;
+}
+
+// src/browser.ts
+function createDeck(options = {}) {
+  return createDeckClient(
+    { allowGuestFallback: false, retryMutations: false, ...options },
+    { resolveAuthUuid, useFetchStreams: true }
+  );
+}
+export {
+  APIError,
+  DECK_TASK_TYPES,
+  DEFAULT_CHUNK_SIZE,
+  DEFAULT_POLL_INTERVAL,
+  DEFAULT_ROOT,
+  DEFAULT_TIMEOUT,
+  INLINE_TASK_FILES_MAX_BYTES,
+  IR_RETENTION_DAYS,
+  PAGE_SEPARATOR,
+  PARSE_PAGED_TASK_TYPES,
+  PARSE_SUPPORTED_EXTENSIONS,
+  PARSE_TASK_TYPE_BY_EXTENSION,
+  RETRY_DELAYS_MS,
+  createDeck,
+  createDeckClient,
+  extensionOf,
+  generateAuthUuid,
+  getRetryDelaysMs,
+  isRetriableAxiosError,
+  isRetriableError,
+  isRetriableHTTPStatus,
+  isValidAuthUuid,
+  parseTaskTypeFor,
+  resetRetryDelaysForTests,
+  resolveAuthUuid,
+  setRetryDelaysForTests
+};
diff --git a/package.json b/package.json
index fdcef410fc9271f593ba5b3f264f95e10139ee55..b7938df740270b4803f95f1d29e4e51d7556903d 100644
--- a/package.json
+++ b/package.json
@@ -10,6 +10,10 @@
     ".": {
       "types": "./dist/index.d.ts",
       "import": "./dist/index.js"
+    },
+    "./browser": {
+      "types": "./dist/browser.d.ts",
+      "import": "./dist/browser.js"
     }
   },
   "files": [
